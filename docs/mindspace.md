### Renaming feature
- Currently auto rename (even though i said only when appropriate)
- Need more rules about renaming, to restrict the name. Also if rename too aggressively would cause user to lose track of their document. Some restrictions include: 1) Dont rename and updated file - probably a working file, and should not change name after each edit. 2) if that an .md, .docx, .xlsx file, with a legible name, then don't change 3) if that a generic name like 'a meeting', then just adjust the name to add more discernable traits like days or the topic 4) only do full rename if that name is totally illegible and is not related in any part to the content (like a downloaded document with names like 'xkdhgksjfs.docx')

### File handlers:
- Need to add these types: img (png, jpg), .xlsx, .csv, pptx

### Implement Claude CLI as a provider

### Capture: if attachment landed right into attachment, then the sibling should be moved out to correct folder

