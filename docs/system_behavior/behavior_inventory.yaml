- id: P1-CAP-01
  behavior: .md file captured in-place with AI summary in frontmatter
  tier: smoke
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.md>
  fixtures:
  - inbox/test-md-capture.md
  steps: '`uv run kms capture inbox/test-md-capture.md`'
  expected:
  - File stays at inbox/test-md-capture.md (not moved)
  - Open file — frontmatter block (between --- markers) added at top
  - 'summary: field present and non-empty'
  - 'tags: includes at least one type/ tag'
  - 'confidence: has a decimal number'
  - Body text below --- unchanged from original
  spec_ref: 'roadmap §Phase1 ''capture pipeline: markdown in-place'''
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_capture_file_md_end_to_end_returns_write_outcome
  status: retired
  retired: '2026-06-09: classify_step now runs on all inbox files. Inbox .md behavior
    covered by P2-CIC-01 (AUTO move), P2-CIC-02 (SUGGEST), P2-CIC-03 (CLUELESS).'
- id: P1-CAP-02
  behavior: PDF already in a project folder creates sibling .md and moves binary to
    attachment/
  tier: smoke
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <Projects/Alpha/sample-report.pdf>
  fixtures:
  - Projects/Alpha/sample-report.pdf
  steps: '`uv run kms capture Projects/Alpha/sample-report.pdf`'
  expected:
  - Projects/Alpha/sample-report.pdf is GONE from Projects/Alpha/ (moved to attachment/)
  - Binary appears at `Projects/Alpha/attachment/sample-report.pdf`
  - Sibling .md at `Projects/Alpha/attachment/.summaries/sample-report.pdf.md`
  - 'Sibling frontmatter has attachment_path: pointing to binary location'
  - 'Sibling frontmatter has type: attachment-summary'
  - 'Sibling frontmatter has summary: (non-empty)'
  spec_ref: roadmap §Phase1 'non-md -> sibling summary'
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_phase12.py::test_store_nonmd_located_happy_path_documents_row
  status: active
  retired: null
- id: P1-CAP-03
  behavior: Body text of .md file preserved exactly after capture (classify_step runs
    on inbox files but does not affect body)
  tier: smoke
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.md>
  fixtures:
  - inbox/test-body-preservation.md
  steps:
  - Note down the body text of inbox/test-body-preservation.md before running
  - '`uv run kms capture inbox/test-body-preservation.md`'
  expected:
  - Body text below second --- is byte-identical to original
  - classify_step runs on this inbox file — candidate fields (suggested_project etc.)
    may appear in frontmatter depending on AI confidence
  - 'summary: appears only inside frontmatter (between --- markers), never in body'
  spec_ref: roadmap §Phase1 'frontmatter-only writes'; updated P2-CIC 2026-06-09
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_handlers/test_markdown_handler.py::test_extract_body_text_matches_known_body
  status: active
  retired: null
- id: P1-CAP-04
  behavior: Re-capture does not rename already-captured .md file (rename gate Rule
    1)
  tier: smoke
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.md> (second run)
  fixtures:
  - inbox/test-rename-gate.md
  steps:
  - 'Run once:'
  - '`uv run kms capture inbox/test-rename-gate.md`'
  - 'Then run again without editing the file:'
  - '`uv run kms capture inbox/test-rename-gate.md`'
  expected:
  - File keeps original name test-rename-gate.md after second capture
  - 'summary: may update but filename unchanged'
  - No duplicate file appears on disk
  spec_ref: 'roadmap §Phase1 ''rename gate: skip already-captured'''
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_rename.py::test_rename_gate_skip_on_recapture
  status: retired
  retired: '2026-06-09: inbox fixture broken by classify_step — first capture may
    AUTO-move file, second capture on old inbox path hits FILE_LOST. Rename gate on
    LOCATED files covered by P2-CIC-04.'
- id: P1-CAP-05
  behavior: Scan captures un-indexed .md files, skips already-indexed
  tier: smoke
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture --scan
  fixtures:
  - inbox/test-md-capture.md
  - inbox/test-scan-uncaptured.md
  steps:
  - 'Both files are in inbox. Capture test-md-capture.md first to get it indexed:'
  - '`uv run kms capture inbox/test-md-capture.md`'
  - 'Now run scan — test-scan-uncaptured.md is in inbox but not yet indexed:'
  - '`uv run kms capture --scan`'
  expected:
  - 'test-scan-uncaptured.md gets summary: in frontmatter'
  - 'test-md-capture.md summary: unchanged (not re-processed)'
  spec_ref: 'roadmap §Phase1 ''scan_capture: added loop'''
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_phase10.py::test_scan_capture_added_unindexed_captured_indexed_unchanged_skipped
  status: retired
  retired: '2026-06-09: scan now runs classify_step on inbox files. Scan behavior
    covered by P2-CIC entries + P2-BAT-04/05.'
- id: P1-CAP-06
  behavior: DOCX file dropped in inbox gets real-summary sibling AND is classified
    at capture time
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <inbox/file.docx>
  fixtures:
  - inbox/q3-planning-brief.docx
  steps: '`uv run kms capture inbox/q3-planning-brief.docx`'
  expected:
  - 'On CLUELESS/SUGGEST: q3-planning-brief.docx stays in inbox/; sibling at inbox/.summaries/'
  - 'On AUTO: binary moves to project attachment/; sibling moves alongside'
  - Sibling .md created at inbox/.summaries/q3-planning-brief.docx.md
  - 'Sibling frontmatter has type: attachment-summary'
  - 'Sibling frontmatter has status: needs-review (NOT pending-routing — concept retired
    in P2-CIC Phase 7)'
  - Sibling frontmatter has source_hash set (idempotent re-entry)
  - Sibling body contains a real AI-generated summary (NOT placeholder text)
  spec_ref: 'roadmap §Phase1 ''handler: docx''; P2-CIC-05 2026-06-09'
  human_reviewed: false
  last_tested: '2026-06-07'
  last_result: passed — behavior changed in P2-CIC Phase 7; re-verify needed
  pytest_ref: tests/test_handlers/test_docx_handler.py::test_extract_returns_success_with_raw_content
  status: active
  retired: null
- id: P1-CAP-07
  behavior: Watcher auto-captures new file drops
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms watch + file created
  fixtures: []
  staging_fixtures:
  - auto-capture-test.md
  steps:
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, copy the staging file to inbox/ and wait ~10 seconds:'
  - '`cp <staging>/auto-capture-test.md <vault>/inbox/`'
  expected:
  - 'auto-capture-test.md gets summary: in frontmatter automatically'
  - No manual kms capture was needed
  - Only one capture pipeline fires per drop (in-flight guard prevents concurrent
    pipelines on same path)
  spec_ref: roadmap §Phase1 'watcher auto-capture'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: null
  status: retired
  retired: '2026-06-09: watcher drops now run classify_step. Watcher capture behavior
    covered by P2-CIC-01/02/03 (inbox) and P2-CIC-04 (LOCATED).'
- id: P1-CAP-08
  behavior: Gibberish-named inbox PDF gets real-summary marker with needs-review status
    (was pending-routing pre-P2-CIC)
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <inbox/gibberish.pdf>
  fixtures:
  - inbox/xkjdhfs83.pdf
  steps: '`uv run kms capture inbox/xkjdhfs83.pdf`'
  expected:
  - xkjdhfs83.pdf stays in inbox/ with original name (no rename during capture)
  - Sibling .md created at inbox/.summaries/xkjdhfs83.pdf.md
  - 'Sibling frontmatter has status: needs-review (NOT pending-routing)'
  - Sibling frontmatter has source_hash set
  - Sibling body has a real AI summary (NOT placeholder)
  - Audit log shows outcome=CLUELESS
  spec_ref: 'roadmap §Phase1 ''CLUELESS path: inbox binary parked'' (DECISION-027);
    updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: '2026-06-07'
  last_result: passed — P2-CIC Phase 7 replaced pending-routing with needs-review;
    re-verify needed
  pytest_ref: tests/test_pipelines/test_capture_rename.py::test_rename_gate_full_rename_on_illegible_binary
  status: retired
  retired: '2026-06-09: classify_step may AUTO-move or SUGGEST instead of CLUELESS.
    Inbox binary classify behavior covered by P2-CIC-05 (AUTO), P2-CIC-03 (CLUELESS).'
- id: P1-CAP-09
  behavior: 'Idempotent capture — unchanged .md file skipped (content_hash match).
    Note: inbox fixture may be AUTO-moved by classify_step on first capture.'
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.md> (same content)
  fixtures:
  - inbox/test-idempotent.md
  steps:
  - 'Run once:'
  - '`uv run kms capture inbox/test-idempotent.md`'
  - 'Then run again without editing the file:'
  - '`uv run kms capture inbox/test-idempotent.md`'
  expected:
  - Second run returns OK (SKIPPED) in terminal output
  - content_hash match — no LLM call made
  - 'summary: field identical to first capture'
  - If file was AUTO-moved by classify_step on first capture, second capture on original
    inbox path returns FILE_LOST
  spec_ref: roadmap §Phase1 'idempotent capture guard'; updated P2-CIC 2026-06-09
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_idempotent_md_unchanged_returns_skipped
  status: active
  retired: null
- id: P1-CAP-10
  behavior: Inbox binary gets real summary AND is classified — may be CLUELESS, SUGGEST,
    or AUTO
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <binary> (no project/domain context)
  fixtures:
  - inbox/mystery-file.pdf
  steps:
  - '`uv run kms capture inbox/mystery-file.pdf`'
  - No project/domain hint in filename or content — capture parks binary + writes
    real-summary sibling.
  expected:
  - 'On CLUELESS/SUGGEST: mystery-file.pdf stays in inbox/; sibling at inbox/.summaries/'
  - 'On AUTO: binary moves to project attachment/; sibling moves alongside'
  - Sibling .md created at inbox/.summaries/mystery-file.pdf.md
  - 'Sibling frontmatter has status: needs-review (NOT pending-routing)'
  - 'Sibling frontmatter has type: attachment-summary'
  - Sibling frontmatter has source_hash set (non-empty — enables idempotent re-entry)
  - Sibling body contains a real AI-generated summary (NOT a one-line placeholder)
  spec_ref: docs/1_design/phase2/classify-inline-capture.md P2-CIC Phase 7 'retire
    pending-routing'
  human_reviewed: false
  last_tested: '2026-06-07'
  last_result: passed — behavior changed in P2-CIC Phase 7; re-verify needed
  pytest_ref: tests/test_pipelines/test_capture_phase3.py::test_store_nonmd_clueless_inbox_binary_stays_needs_review
  status: active
  retired: null
- id: P1-CAP-11
  behavior: URL enrichment — sparse note with URLs gets content fetched (classify_step
    also runs on inbox files)
  tier: phase
  phase: 1
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.md> (body has URLs, < 500 chars)
  fixture_body: 'Quick note — read this later: https://example.com


    Test fixture for P1-CAP-11.

    '
  fixtures:
  - inbox/test-url-note.md
  steps:
  - '`uv run kms capture inbox/test-url-note.md`'
  - File body must contain a URL and less than 500 chars of text to trigger URL enrichment.
  expected:
  - 'Open file — summary: reflects content from the URL, not just the sparse body
    text'
  - classify_step runs — candidate fields or AUTO-move possible
  spec_ref: roadmap §Phase1 'enrich_urls stage'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_enrich_urls_sparse_text_fetches_and_augments
  status: active
  retired: null
- id: P1-DEV-01
  behavior: Audit trail written for every capture decision
  tier: full
  phase: 1
  origin: implementation
  granularity: mechanism
  trigger: kms capture <any file>
  fixtures:
  - inbox/p1-dev-01-test.md
  steps:
  - 'Capture the test file:'
  - '`uv run kms capture inbox/p1-dev-01-test.md`'
  - 'Then query:'
  - '`sqlite3 data/kb.db "SELECT stage, outcome FROM audit_log ORDER BY rowid DESC
    LIMIT 5"`'
  expected:
  - audit_log row exists with stage=metadata, outcome=CAPTURED
  - audit_log row exists with stage=classify (AUTO, SUGGEST, or CLUELESS — classify_step
    runs on inbox files)
  - correlation_id matches the ID shown in terminal output
  - reasoning field is non-empty in both rows
  spec_ref: CONSTRAINTS.md C-12 'audit log non-negotiable'; P2-CIC-06 2026-06-09
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_metadata_writes_audit_log_row
  status: active
  retired: null
- id: P1-DEV-02
  behavior: Tag violation audited — invalid tags stripped and logged
  tier: full
  phase: 1
  origin: implementation
  granularity: mechanism
  trigger: kms capture <file.md> (LLM returns bad tag)
  fixtures: []
  steps:
  - 'Force a capture where LLM returns a tag not in tags.yaml taxonomy (developer
    test — requires mocking LLM response). Then query the audit log:'
  - '`sqlite3 data/kb.db "SELECT stage, outcome, reasoning FROM audit_log WHERE stage=''tag_violation''
    ORDER BY rowid DESC LIMIT 3"`'
  expected:
  - audit_log row with stage=tag_violation, outcome=TAG_VIOLATION
  - Bad tag removed from final tags list in frontmatter
  - Valid tags preserved in frontmatter
  spec_ref: roadmap §Phase1 'tag validation against taxonomy'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_metadata_with_violations_writes_two_audit_rows
  status: active
  retired: null
- id: P1-DEV-03
  behavior: File-lost guard — deleted file between event and pipeline
  tier: full
  phase: 1
  origin: implementation
  granularity: mechanism
  trigger: kms capture <path> (file deleted before pipeline reads)
  fixtures: []
  steps:
  - 'Run capture on a path that does not exist on disk:'
  - '`uv run kms capture inbox/nonexistent-file.md`'
  - 'Then verify audit log entry and no DB row created:'
  - '`sqlite3 data/kb.db "SELECT stage, outcome FROM audit_log WHERE stage=''file_lost''
    ORDER BY rowid DESC LIMIT 3"`'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path=''inbox/nonexistent-file.md''"`'
  expected:
  - Terminal shows Failure(recoverable=True)
  - audit_log row with stage=file_lost, outcome=FILE_LOST
  - No DB row created in documents table
  spec_ref: roadmap §Phase1.5 'FILE_LOST guard'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_capture_file_entry_guard_fires_when_file_not_found
  status: active
  retired: null
- id: P1-DEV-04
  behavior: Cooldown gate rejects file still being written
  tier: full
  phase: 1
  origin: implementation
  granularity: mechanism
  trigger: kms capture <file> (mtime < cooldown_seconds ago)
  fixtures: []
  steps:
  - 'Write a new file to inbox/, then immediately run capture (within the cooldown
    window, before mtime ages):'
  - '`uv run kms capture inbox/<that-file>.md`'
  - 'Then verify rejection was logged (look for ''cooldown'' or ''too recent'' in
    logs):'
  - '`grep -i ''cooldown\|too_recent\|file_too_recent'' logs/app.log | tail -5`'
  expected:
  - Terminal shows Failure(recoverable=True)
  - No LLM call made
  - No DB row created in documents table
  spec_ref: roadmap §Phase1 'cooldown gate'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_phase3.py::test_capture_file_rejects_too_recent_file
  status: active
  retired: null
- id: P1-DEV-05
  behavior: Pending-routing guard blocks re-capture of CLUELESS binary
  tier: full
  phase: 1
  origin: implementation
  granularity: mechanism
  trigger: kms capture <binary> (already has pending-routing marker)
  fixtures:
  - inbox/mystery-file.pdf
  steps:
  - 'Run once to create the CLUELESS marker:'
  - '`uv run kms capture inbox/mystery-file.pdf`'
  - 'Then run again without changing anything:'
  - '`uv run kms capture inbox/mystery-file.pdf`'
  - 'Verify only one marker row exists in DB (count should be 1):'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path LIKE ''%mystery-file.pdf%''"`'
  expected:
  - Second capture returns Failure(recoverable=True)
  - No duplicate marker .md created
  - No LLM call made on second run
  spec_ref: roadmap §Phase1.5 'pending-routing guard'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_phase3.py::test_pending_routing_guard_removed
  status: retired
  retired: '2026-06-09: P2-CIC Phase 7 removed the pending-routing guard entirely
    — old status:pending-routing concept retired. Idempotent re-entry now uses source_hash
    comparison in _store_nonmd CLUELESS path (capture.py). New test asserts the OPPOSITE:
    classify IS called on second run (guard removed). See P2-CIC-03 for current CLUELESS
    binary behavior.'
- id: P15-LOC-01
  behavior: Note in Domain/ folder gets domain/<D> tag
  tier: smoke
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <Domain/Finance/note.md>
  fixtures:
  - Domain/Finance/test-domain-tag.md
  steps: '`uv run kms capture Domain/Finance/test-domain-tag.md`'
  expected:
  - 'Open file — frontmatter tags: contains domain/Finance'
  - 'No domain: scalar field in frontmatter (only the tag)'
  spec_ref: 'roadmap §Phase1.5 ''apply_location_tags: domain'''
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_apply_location_tags_domain_file_adds_tag
  status: active
  retired: null
- id: P15-LOC-02
  behavior: Note in Projects/ folder gets project field
  tier: smoke
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <Projects/Alpha/note.md>
  fixtures:
  - Projects/Alpha/test-project-tag.md
  steps: '`uv run kms capture Projects/Alpha/test-project-tag.md`'
  expected:
  - 'Open file — frontmatter has project: Alpha'
  spec_ref: 'roadmap §Phase1.5 ''apply_location_tags: project'''
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_apply_location_tags_project_file_sets_ai_project
  status: active
  retired: null
- id: P15-REC-01
  behavior: Reconcile removes stale domain tag when folder deleted
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms reconcile
  fixture_frontmatter: 'type: capture

    tags:

    - domain/OldDomain

    - type/capture

    '
  fixtures:
  - inbox/test-stale-domain-tag.md
  steps:
  - 'Place inbox/test-stale-domain-tag.md with a domain/OldDomain tag in frontmatter
    (but no Domain/OldDomain/ folder exists on disk). Then run reconcile:'
  - '`uv run kms reconcile`'
  expected:
  - Open file — domain/OldDomain tag removed from frontmatter
  - Other tags preserved
  spec_ref: 'roadmap §Phase1.5 ''reconcile_stale_tags: remove stale'''
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_removes_stale_domain_tag
  status: active
  retired: null
- id: P15-REC-02
  behavior: Reconcile adds missing domain tag for note in Domain/ folder
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Domain/Engineering/test-missing-domain-tag.md
  steps:
  - 'Place Domain/Engineering/test-missing-domain-tag.md without a domain/Engineering
    tag in frontmatter. Then run reconcile:'
  - '`uv run kms reconcile`'
  expected:
  - Open file — domain/Engineering tag added to frontmatter
  - Body text unchanged
  spec_ref: 'roadmap §Phase1.5 ''reconcile_stale_tags: add missing'''
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_adds_missing_domain_tag
  status: active
  retired: null
- id: P15-REC-03
  behavior: Reconcile captures orphan binaries missing sibling .md
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/attachment/orphan-report.pdf
  steps:
  - 'Place Projects/Alpha/attachment/orphan-report.pdf on disk without any .summaries/orphan-report.pdf.md
    sibling. Then run reconcile:'
  - '`uv run kms reconcile`'
  expected:
  - Sibling .md created at Projects/Alpha/attachment/.summaries/orphan-report.pdf.md
  - 'Sibling frontmatter has summary: (non-empty)'
  - 'Sibling frontmatter has attachment_path: pointing to orphan-report.pdf'
  spec_ref: roadmap §Phase1.5 'reconcile_orphan_binaries'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_orphan_binaries_captures_binary_without_sibling
  status: active
  retired: null
- id: P15-REC-04
  behavior: Reconcile deletes orphan sibling when binary is gone
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/attachment/.summaries/deleted-file.pdf.md
  fixture_frontmatter: 'type: attachment-summary

    attachment_path: Projects/Alpha/attachment/deleted-file.pdf

    '
  steps:
  - 'Place Projects/Alpha/attachment/.summaries/deleted-file.pdf.md on disk (with
    type: attachment-summary in frontmatter) but NO corresponding binary at the attachment_path.
    Then run reconcile:'
  - '`uv run kms reconcile`'
  - 'Verify sibling deleted from disk (should return nothing):'
  - '`ls <vault>/Projects/Alpha/attachment/.summaries/deleted-file.pdf.md 2>&1`'
  - 'Verify DB row removed (expected: 0):'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path=''Projects/Alpha/attachment/.summaries/deleted-file.pdf.md''"`'
  expected:
  - Orphan sibling deleted-file.pdf.md deleted from disk
  - DB row for deleted-file.pdf.md removed from documents table
  spec_ref: roadmap §Phase1.5 'reconcile_orphan_siblings'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: failed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_orphan_siblings_removes_ghost_when_binary_gone
  status: active
  retired: null
- id: P15-REC-05
  behavior: Reconcile clears stale batch_id when doc moved away from batch destination
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/note-batch-test.md
  steps:
  - '**Prerequisite:** TD-040 must be resolved — single-file capture must set batch_id.'
  - '**Step 1 — capture the file to seed a batch_id:**'
  - '`uv run kms capture <vault>/Projects/Alpha/note-batch-test.md`'
  - '**Step 2 — verify batch_id is set:**'
  - '`sqlite3 data/kb.db "SELECT vault_path, batch_id FROM documents WHERE vault_path
    LIKE ''%note-batch-test%''"`'
  - Confirm batch_id column shows a non-NULL integer.
  - '**Step 3 — move the file to a different project:**'
  - '`mkdir -p <vault>/Projects/Beta && mv <vault>/Projects/Alpha/note-batch-test.md
    <vault>/Projects/Beta/note-batch-test.md`'
  - '**Step 4 — run reconcile:**'
  - '`uv run kms reconcile`'
  - '**Step 5 — verify batch_id cleared:**'
  - '`sqlite3 data/kb.db "SELECT vault_path, batch_id FROM documents WHERE vault_path
    LIKE ''%note-batch-test%''"`'
  expected:
  - 'After Step 2: batch_id is a non-NULL integer for Projects/Alpha/note-batch-test.md'
  - 'After Step 5: batch_id is NULL for Projects/Beta/note-batch-test.md'
  spec_ref: roadmap §Phase1.5 'reconcile_stale_batch_refs'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: failed - no single-file capturing with batch_id yet (application gap)
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_stale_batch_refs_file_moved_away
  status: active
  retired: null
- id: P15-HDL-01
  behavior: XLSX file dropped in inbox gets real-summary marker with needs-review
    status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.xlsx>
  fixtures:
  - inbox/q2-budget.xlsx
  steps: '`uv run kms capture inbox/q2-budget.xlsx`'
  expected:
  - q2-budget.xlsx stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/q2-budget.xlsx.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing — concept retired
    in P2-CIC Phase 7)'
  - Marker frontmatter has source_hash set (idempotent re-entry)
  - Marker body contains a real AI-generated summary (NOT placeholder)
  spec_ref: 'roadmap §Phase1.5 ''handler: xlsx''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_xlsx_handler.py::test_single_sheet_has_data_rows
  status: active
  retired: null
- id: P15-HDL-02
  behavior: PPTX file dropped in inbox gets real-summary marker with needs-review
    status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.pptx>
  fixtures:
  - inbox/deck.pptx
  steps: '`uv run kms capture inbox/deck.pptx`'
  expected:
  - deck.pptx stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/deck.pptx.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing)'
  - Marker frontmatter has source_hash set (idempotent re-entry)
  - Marker body contains a real AI-generated summary
  spec_ref: 'roadmap §Phase1.5 ''handler: pptx''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_pptx_handler.py::test_slide_without_title_has_plain_header
  status: active
  retired: null
- id: P15-HDL-03
  behavior: CSV file dropped in inbox gets real-summary marker with needs-review status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.csv>
  fixtures:
  - inbox/data.csv
  steps: '`uv run kms capture inbox/data.csv`'
  expected:
  - data.csv stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/data.csv.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing)'
  - Marker frontmatter has source_hash set
  - Marker body contains a real AI-generated summary
  spec_ref: 'roadmap §Phase1.5 ''handler: csv''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_csv_handler.py::test_valid_csv_returns_success
  status: active
  retired: null
- id: P15-HDL-04
  behavior: HTML file dropped in inbox gets real-summary marker with needs-review
    status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.html>
  fixtures:
  - inbox/page.html
  steps: '`uv run kms capture inbox/page.html`'
  expected:
  - page.html stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/page.html.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing)'
  - Marker frontmatter has source_hash set
  - Marker body contains a real AI-generated summary
  spec_ref: 'roadmap §Phase1.5 ''handler: html''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_html_handler.py::test_valid_html_returns_success
  status: active
  retired: null
- id: P15-HDL-05
  behavior: EML email file dropped in inbox gets real-summary marker with needs-review
    status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.eml>
  fixtures:
  - inbox/message.eml
  steps: '`uv run kms capture inbox/message.eml`'
  expected:
  - message.eml stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/message.eml.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing)'
  - Marker frontmatter has source_hash set
  - Marker body contains a real AI-generated summary
  spec_ref: 'roadmap §Phase1.5 ''handler: eml''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_eml_handler.py::test_simple_eml_returns_success
  status: active
  retired: null
- id: P15-HDL-06
  behavior: MSG Outlook file dropped in inbox gets real-summary marker with needs-review
    status
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture <file.msg>
  fixtures:
  - inbox/outlook-msg.msg
  steps: '`uv run kms capture inbox/outlook-msg.msg`'
  expected:
  - outlook-msg.msg stays in inbox/ (not moved — CLUELESS path)
  - Marker .md created at inbox/.summaries/outlook-msg.msg.md
  - 'Marker frontmatter has type: attachment-summary'
  - 'Marker frontmatter has status: needs-review (NOT pending-routing)'
  - Marker frontmatter has source_hash set
  - Marker body contains a real AI-generated summary
  spec_ref: 'roadmap §Phase1.5 ''handler: msg''; updated P2-CIC Phase 7 2026-06-09'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_handlers/test_msg_handler.py::test_valid_msg_returns_success
  status: active
  retired: null
- id: P15-HDL-07
  behavior: PNG/JPG image file captured with sibling .md
  tier: phase
  phase: 1.5
  origin: design
  granularity: outcome
  trigger: kms capture <file.png>
  fixtures:
  - inbox/screenshot.png
  steps: '`uv run kms capture inbox/screenshot.png`'
  expected:
  - Sibling .md created with summary (OCR text or filename-based)
  expected_design: 'Sibling .md created with summary (OCR text or filename-based).

    '
  expected_implementation: 'NOT implemented. PngHandler/JpgHandler.extract() return
    Failure("image extraction requires a vision-capable LLM — not yet implemented",
    recoverable=False) at src/handlers/image_handler.py:21-26 and :30-38. No sibling
    .md is created; no summary is produced. Images are NOT captured.

    '
  spec_ref: 'roadmap §Phase1.5 ''handler: image'''
  human_reviewed: true
  last_tested: '2026-06-05'
  last_result: failed - img LLM not yet implemented
  pytest_ref: tests/test_handlers/test_image_handler.py::test_always_returns_failure
  status: conflict
  retired: null
- id: P15-FOLD-01
  behavior: Folder dropped in inbox classified and routed by LLM
  tier: phase
  phase: 1.5
  origin: implementation
  granularity: outcome
  trigger: kms capture --scan (folder in inbox/) or watcher folder event
  fixtures:
  - inbox/new-project-folder/file1.md
  - inbox/new-project-folder/file2.pdf
  steps:
  - 'Create inbox/new-project-folder/ with inbox/new-project-folder/file1.md and inbox/new-project-folder/file2.pdf
    inside. Then run scan:'
  - '`uv run kms capture --scan`'
  - 'Verify batches row created:'
  - '`sqlite3 data/kb.db "SELECT folder_name, destination_type, destination_name,
    confidence, status FROM batches ORDER BY rowid DESC LIMIT 5"`'
  - 'If status=ROUTING: verify files captured with batch_id:'
  - '`sqlite3 data/kb.db "SELECT vault_path, batch_id FROM documents WHERE batch_id
    = (SELECT batch_id FROM batches ORDER BY rowid DESC LIMIT 1)"`'
  expected:
  - 'batches row created with folder_name=new-project-folder, confidence > 0, status
    one of: ROUTING | PENDING_REVIEW | CLUELESS'
  - 'AUTO (status=ROUTING): folder moved to Projects/<name>/ or Domain/<name>/; files
    captured; documents rows present with matching batch_id'
  - 'SUGGEST (status=PENDING_REVIEW): folder stays in inbox/; no documents rows for
    its files yet'
  - 'CLUELESS (status=CLUELESS): folder stays in inbox/; per-file needs-review markers
    with real summaries at inbox/.summaries/; documents rows with batch_id'
  - All files inside the folder (including subfolders of Projects/<A>/ or Domain/<D>/)
    captured with a batch_id
  spec_ref: roadmap §Phase1.5 'folder drop classification' — see TD-041 for scan gap
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: failed
  pytest_ref: tests/test_pipelines/test_capture_folder.py::test_inbox_auto_confidence_moves_folder_and_captures_files
  status: active
  retired: null
- id: P15-DEV-01
  behavior: Watcher binary-delete sync — sibling cleaned up
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: Delete a binary file while kms watch running
  fixtures:
  - Projects/Alpha/attachment/p15-dev-01-report.pdf
  steps:
  - 'First, capture the binary to establish sibling in DB:'
  - '`uv run kms capture Projects/Alpha/attachment/p15-dev-01-report.pdf`'
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, delete the binary and wait ~5 seconds:'
  - '`rm $VAULT/Projects/Alpha/attachment/p15-dev-01-report.pdf`'
  - 'In Terminal 2, verify sibling removed from DB (count should be 0) and audit entry
    written:'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path LIKE ''%p15-dev-01-report.pdf.md%''"`'
  - '`sqlite3 data/kb.db "SELECT stage, outcome FROM audit_log WHERE stage=''watcher:binary_delete''
    ORDER BY rowid DESC LIMIT 3"`'
  expected:
  - Sibling .md at `.summaries/<binary.name>.md` deleted from disk
  - DB row for sibling removed from documents table
  - audit_log row with stage=watcher:binary_delete, outcome=SIBLING_ORPHANED
  spec_ref: roadmap §Phase1.5 'binary sync on delete'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_vault/test_watcher.py::test_on_deleted_binary_removes_sibling_from_db
  status: active
  retired: null
- id: P15-DEV-02
  behavior: Watcher binary-rename sync — sibling renamed
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: Rename a binary file while kms watch running
  fixtures:
  - Projects/Alpha/attachment/p15-dev-02-old.pdf
  steps:
  - 'First, capture the binary to establish sibling in DB:'
  - '`uv run kms capture Projects/Alpha/attachment/p15-dev-02-old.pdf`'
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, rename the binary and wait ~5 seconds:'
  - '`mv $VAULT/Projects/Alpha/attachment/p15-dev-02-old.pdf $VAULT/Projects/Alpha/attachment/p15-dev-02-new.pdf`'
  - 'In Terminal 2, verify DB row updated with new path and attachment_path:'
  - '`sqlite3 data/kb.db "SELECT vault_path, attachment_path FROM documents WHERE
    vault_path LIKE ''%p15-dev-02-new.pdf.md%''"`'
  expected:
  - Sibling renamed from old.pdf.md to new.pdf.md in .summaries/
  - Open sibling — attachment_path in frontmatter updated to new.pdf
  - DB row path and attachment_path both updated
  spec_ref: roadmap §Phase1.5 'binary sync on rename'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_vault/test_watcher.py::test_on_moved_binary_same_folder_renames_sibling
  status: active
  retired: null
- id: P15-DEV-03
  behavior: Watcher binary cross-folder move — old sibling orphaned
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: Move binary between attachment/ folders while kms watch running
  fixtures:
  - Projects/Alpha/attachment/p15-dev-03-file.pdf
  steps:
  - 'First, capture the binary to establish sibling in DB:'
  - '`uv run kms capture Projects/Alpha/attachment/p15-dev-03-file.pdf`'
  - 'Create the destination folder:'
  - '`mkdir -p $VAULT/Projects/Beta/attachment`'
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, move the binary to a different project''s attachment/ and wait
    ~5 seconds:'
  - '`mv $VAULT/Projects/Alpha/attachment/p15-dev-03-file.pdf $VAULT/Projects/Beta/attachment/p15-dev-03-file.pdf`'
  - 'In Terminal 2, verify old sibling DB row removed (count should be 0):'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path LIKE ''%Projects/Alpha/attachment/.summaries/p15-dev-03-file.pdf.md%''"`'
  expected:
  - Old sibling .md in Projects/Alpha/attachment/.summaries/ deleted from disk
  - DB row for old sibling removed (count = 0)
  - New orphan binary in Projects/Beta/attachment/ picked up on next `uv run kms reconcile`
  spec_ref: roadmap §Phase1.5 'binary sync cross-folder'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_vault/test_watcher.py::test_on_moved_binary_different_folder_orphans_old_sibling
  status: active
  retired: null
- id: P15-DEV-04
  behavior: Reconcile stage 3 — re-summarize stale binaries
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/attachment/p15-dev-04-report.pdf
  steps:
  - 'First, capture the binary to establish sibling in DB and record the initial content_hash:'
  - '`uv run kms capture Projects/Alpha/attachment/p15-dev-04-report.pdf`'
  - '`sqlite3 data/kb.db "SELECT vault_path, content_hash FROM documents WHERE vault_path
    LIKE ''%p15-dev-04-report.pdf.md%''"`'
  - 'Touch the binary to make it newer than the sibling .md:'
  - '`touch $VAULT/Projects/Alpha/attachment/p15-dev-04-report.pdf`'
  - 'Run reconcile:'
  - '`uv run kms reconcile`'
  - 'Verify sibling DB row has an updated content_hash:'
  - '`sqlite3 data/kb.db "SELECT vault_path, content_hash FROM documents WHERE vault_path
    LIKE ''%p15-dev-04-report.pdf.md%''"`'
  expected:
  - Sibling .md re-summarized with updated content
  - DB row updated with new content_hash
  spec_ref: roadmap §Phase1.5 'reconcile_stale_binaries'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_binaries_recaptures_when_binary_newer
  status: active
  retired: null
- id: P15-DEV-05
  behavior: Debounce coalescing — rapid file events produce single capture
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: kms watch + rapid file saves
  fixtures:
  - inbox/p15-dev-05-debounce.md
  steps:
  - 'Clear any prior DB row for the test file:'
  - '`sqlite3 data/kb.db "DELETE FROM documents WHERE vault_path=''inbox/p15-dev-05-debounce.md''"`'
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, touch the file 5 times within 1 second:'
  - '`for i in 1 2 3 4 5; do touch $VAULT/inbox/p15-dev-05-debounce.md; done`'
  - 'After the debounce window (~3 sec), verify only one DB row exists (count should
    be 1):'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path=''inbox/p15-dev-05-debounce.md''"`'
  expected:
  - Only one capture_file call fires (after 3.0s debounce window)
  - No duplicate DB rows in documents table
  spec_ref: roadmap §Phase1 'watcher debounce'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_vault/test_watcher.py::test_debounce_rapid_creates_fire_once
  status: active
  retired: null
- id: P15-DEV-06
  behavior: Scan skips .summaries/ paths in added loop
  tier: full
  phase: 1.5
  origin: implementation
  granularity: mechanism
  trigger: kms capture --scan
  fixtures:
  - Projects/Alpha/attachment/.summaries/p15-dev-06-test.pdf.md
  fixture_frontmatter: 'type: attachment-summary

    attachment_path: Projects/Alpha/attachment/p15-dev-06-test.pdf

    '
  steps:
  - 'Run scan:'
  - '`uv run kms capture --scan`'
  - 'Verify the sibling fixture was NOT captured as a new document (count should be
    0):'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path=''Projects/Alpha/attachment/.summaries/p15-dev-06-test.pdf.md''"`'
  expected:
  - Sibling .md files inside .summaries/ NOT re-captured by scan
  - No DB row created for p15-dev-06-test.pdf.md (count = 0)
  - No rename or identity wipe on existing siblings
  spec_ref: CLAUDE.md 'scan_capture modified loop skips .summaries/'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_phase9.py::test_scan_capture_skips_summaries_in_modified_loop
  status: active
  retired: null
- id: P15-DEV-07
  behavior: In-flight guard — modify event during running pipeline does not launch
    second capture
  tier: full
  phase: 1.5
  origin: bugfix
  granularity: mechanism
  trigger: kms watch + on_modified fires while on_created pipeline still running (gap
    > debounce window)
  fixtures:
  - inbox/sample.md
  steps:
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - 'In Terminal 2, drop file and wait for pipeline to start (but not finish), then
    touch it:'
  - '`cp tests/fixtures/sample.md <vault>/inbox/ && sleep 5 && touch <vault>/inbox/sample.md`'
  - 'In Terminal 2, check logs for the skip marker and verify only one capture fired:'
  - '`grep ''skip_in_flight'' logs/app.log | tail -5`'
  - '`grep ''metadata.captured'' logs/app.log | tail -5`'
  - 'Verify only one audit entry for this path:'
  - '`sqlite3 data/kb.db "SELECT count(*), stage, outcome FROM audit_log WHERE outcome=''CAPTURED''
    GROUP BY stage, outcome ORDER BY rowid DESC LIMIT 5"`'
  expected:
  - watcher.skip_in_flight debug log appears for the modify event
  - Only one metadata.captured log line appears
  - No duplicate audit entries for the same path
  spec_ref: cli/main.py _dispatch() in-flight set
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_cloud_entry.py::test_health_returns_200_without_key
  status: active
  retired: null
- id: PRE2-DB-01
  behavior: Capture populates project, status, key_topics DB columns
  tier: phase
  phase: pre-2
  origin: implementation
  granularity: mechanism
  trigger: kms capture <Projects/Alpha/note.md>
  fixtures:
  - Projects/Alpha/test-db-columns.md
  steps:
  - '`uv run kms capture Projects/Alpha/test-db-columns.md`'
  - 'Then query the DB to verify columns:'
  - '`sqlite3 data/kb.db "SELECT vault_path, project, status, key_topics FROM documents
    WHERE vault_path LIKE ''%test-db-columns%''"`'
  expected:
  - DB row has project=Alpha
  - key_topics contains a JSON array of topic tags
  - status column present (may be NULL)
  spec_ref: roadmap §Phase-Pre-2 'TD-008 documents columns'
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_phase12.py::test_store_nonmd_located_happy_path_documents_row
  status: active
  retired: null
- id: PRE2-DOM-01
  behavior: 'Old domain: scalar stripped by reconcile (lazy migration — Stage 5)'
  tier: phase
  phase: pre-2
  origin: implementation
  granularity: outcome
  trigger: kms reconcile (Stage 5 — stale-tags sweep writes file, dumps() strips deprecated
    key)
  fixtures:
  - Domain/Finance/test-pre2-dom-01.md
  fixture_frontmatter: 'domain: finance

    tags:

    - domain/Finance

    - type/capture

    '
  steps:
  - 'Run reconcile:'
  - '`uv run kms reconcile`'
  - 'Open `Domain/Finance/test-pre2-dom-01.md` and inspect frontmatter:'
  expected:
  - 'domain: scalar key GONE from frontmatter'
  - 'domain/Finance still present in tags: list'
  - 'type/capture still present in tags: list'
  - Body content unchanged
  spec_ref: roadmap §Phase-Pre-2 'TD-038 deprecated keys filter'; TD-042 (Stage 5
    must mark dirty for deprecated keys)
  human_reviewed: true
  last_tested: '2026-06-07'
  last_result: failed - all frontmatter changed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_strips_deprecated_key
  status: active
  retired: null
- id: VR-PLACE-01
  behavior: No-edit binary (PDF/PNG/JPG) routed to attachment/
  tier: phase
  phase: vault-restructure
  origin: implementation
  granularity: outcome
  trigger: kms capture <no-edit binary>
  fixtures:
  - Projects/Alpha/vr-place-01-test.pdf
  steps:
  - '`uv run kms capture Projects/Alpha/vr-place-01-test.pdf`'
  expected:
  - vr-place-01-test.pdf GONE from Projects/Alpha/ (moved to attachment/)
  - Binary appears at Projects/Alpha/attachment/vr-place-01-test.pdf
  - Sibling .md at Projects/Alpha/attachment/.summaries/vr-place-01-test.pdf.md
  - 'Sibling frontmatter has type: attachment-summary'
  spec_ref: ADR-0006 'no-edit → attachment/'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_noedit_in_attachment_no_move
  status: active
  retired: null
- id: VR-PLACE-02
  behavior: Editable binary (XLSX/DOCX/PPTX) routed to project/domain root
  tier: phase
  phase: vault-restructure
  origin: implementation
  granularity: outcome
  trigger: kms capture <editable binary in Projects/>
  fixtures:
  - Projects/Alpha/vr-place-02-test.xlsx
  steps:
  - 'Run capture on the editable binary:'
  - '`uv run kms capture Projects/Alpha/vr-place-02-test.xlsx`'
  expected:
  - Binary still at Projects/Alpha/vr-place-02-test.xlsx (NOT moved to attachment/)
  - Sibling .md written (NOT under attachment/ — editable stays at root)
  spec_ref: ADR-0006 'editable → root'
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture.py::test_editable_in_attachment_moved_to_root
  status: active
  retired: null
- id: VR-SKIP-01
  behavior: Watcher and scan skip AI-output folders (Briefings/Synthesis/Documentation)
  tier: phase
  phase: vault-restructure
  origin: implementation
  granularity: outcome
  trigger: kms capture --scan or kms watch
  fixtures:
  - Briefings/vr-skip-01-test.md
  steps:
  - 'Run scan:'
  - '`uv run kms capture --scan`'
  - 'Verify no DB row created for the Briefings/ file:'
  - '`sqlite3 data/kb.db "SELECT count(*) FROM documents WHERE vault_path LIKE ''Briefings/%''"`'
  expected:
  - File in Briefings/ NOT captured (skipped)
  - No DB row created in documents table for the file (count = 0)
  spec_ref: ADR-0006 'capture-excluded folders'
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: Passed
  pytest_ref: tests/test_vault/test_watcher.py::test_should_skip_logs_debug_for_ai_output
  status: active
  retired: null
- id: VR-REHOME-01
  behavior: Misplaced binary re-homed to correct placement
  tier: phase
  phase: vault-restructure
  origin: implementation
  granularity: outcome
  trigger: kms reconcile (stage 7)
  fixtures:
  - Projects/Alpha/attachment/vr-rehome-01-test.xlsx
  steps:
  - 'Run reconcile (stage 7 detects editable binary in attachment/ and moves it to
    root):'
  - '`uv run kms reconcile`'
  expected:
  - vr-rehome-01-test.xlsx GONE from Projects/Alpha/attachment/
  - File appears at Projects/Alpha/vr-rehome-01-test.xlsx (project root, visible in
    Obsidian)
  - Sibling .md path updated to reflect new location
  spec_ref: ADR-0006 'reconcile_editable_migration'
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile_editable_migration.py::test_editable_to_no_edit_migration
  status: active
  retired: null
- id: VR-CONTENT-01
  behavior: Binary content change detected and re-summarized
  tier: full
  phase: vault-restructure
  origin: implementation
  granularity: mechanism
  trigger: kms watch + binary file edited
  fixtures:
  - Projects/Alpha/attachment/vr-content-01-test.xlsx
  steps:
  - 'Ensure the binary has been captured first (creates sibling in .summaries/):'
  - '`uv run kms capture Projects/Alpha/attachment/vr-content-01-test.xlsx`'
  - 'Record the current content_hash:'
  - '`sqlite3 data/kb.db "SELECT vault_path, content_hash FROM documents WHERE vault_path
    LIKE ''%vr-content-01%''"`'
  - 'In Terminal 1, start the watcher:'
  - '`uv run kms watch`'
  - In Terminal 2, edit the XLSX content (actual data change, not just touch) and
    wait ~5 seconds for watcher to detect SHA-256 change.
  - 'Verify content_hash changed:'
  - '`sqlite3 data/kb.db "SELECT vault_path, content_hash FROM documents WHERE vault_path
    LIKE ''%vr-content-01%''"`'
  expected:
  - Sibling .md re-summarized with updated content
  - source_hash updated in sibling frontmatter (open .summaries/vr-content-01-test.xlsx.md
    to verify)
  - content_hash in DB row differs from pre-edit value
  spec_ref: ADR-0006 'content_change_detect'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_vault/test_watcher_binary_modify.py::test_hash_differs_updates_source_hash
  status: active
  retired: null
- id: P2-BAT-01
  behavior: Single file captured into a project subfolder gets batch_id set in the
    documents row
  tier: full
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <Projects/Alpha/subdir/report.pdf>
  fixtures: []
  steps: 'Drop a file into Projects/Alpha/subdir/ and run `uv run kms capture Projects/Alpha/subdir/report.pdf`.
    Query the DB: `sqlite3 data/kb.db ''SELECT vault_path, batch_id FROM documents
    WHERE vault_path LIKE "%report%"''`'
  expected:
  - batch_id column is non-NULL; the value references a row in the batches table with
    folder_path = 'Projects/Alpha/subdir'
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_batch_stamp_sets_batch_id_on_subfolder_capture
  status: active
  retired: null
- id: P2-BAT-02
  behavior: Two files dropped separately into the same subfolder share the same batch_id
  tier: full
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <file1>; kms capture <file2> (same folder)
  fixtures: []
  steps: Capture two files from the same subfolder in separate CLI invocations. Query
    batch_id for both vault_path rows.
  expected:
  - Both documents rows have identical batch_id values; the batches row for that folder_path
    has file_count >= 2
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture.py::test_batch_stamp_reuses_existing_batch
  status: active
  retired: null
- id: P2-BAT-03
  behavior: Single file captured directly into inbox root or vault root gets no batch_id
  tier: full
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/report.pdf>
  fixtures: []
  steps: Capture a file dropped directly into inbox/ (not inside a subfolder). Query
    batch_id for the resulting documents row.
  expected:
  - batch_id column is NULL — inbox root is not batch-worthy
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_folder.py::test_single_file_capture_leaves_batch_id_null
  status: active
  retired: null
- id: P2-BAT-04
  behavior: scan_capture detects and batch-captures an unprocessed inbox subfolder
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture --scan
  fixtures:
  - inbox/test-batch-drop/note-a.md
  - inbox/test-batch-drop/note-b.md
  steps:
  - 'Create the subfolder and two files: `mkdir -p <vault>/inbox/test-batch-drop`
    then drop any two .md files inside it (can be blank or with a line of text).'
  - Run `uv run kms capture --scan` from the repo root.
  - 'Check both files were indexed: `sqlite3 data/kb.db "SELECT vault_path, batch_id
    FROM documents WHERE vault_path LIKE ''inbox/test-batch-drop/%'' ORDER BY vault_path"`'
  - 'Check a batch row was created: `sqlite3 data/kb.db "SELECT folder_path, status
    FROM batches WHERE folder_path LIKE ''%test-batch-drop%''"`'
  expected:
  - Both vault_path rows returned by the first query, both with the same non-NULL
    batch_id (a UUID-style string).
  - The batches query returns exactly one row with folder_path containing 'test-batch-drop'.
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_folder.py::test_project_drop_writes_batch_id_on_document_rows
  status: active
  retired: null
- id: P2-BAT-05
  behavior: scan_capture skips a subfolder already captured by the watcher (no duplicate
    batch)
  tier: full
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture --scan (after watcher already processed the folder)
  fixtures: []
  steps: Start watcher, drop a subfolder into inbox/. After watcher captures it, stop
    watcher and run `kms capture --scan`.
  expected:
  - No second batches row created for the same folder_path. Existing documents rows
    unchanged. Log shows subfolder skipped.
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_capture_folder.py::test_folder_path_nfc_matches_batch_lookup
  status: active
  retired: null
- id: P2-BAT-06
  behavior: File moved into a batch-worthy subfolder by the watcher gets batch_id
    updated in-place
  tier: full
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms watch + file moved into Projects/Alpha/subdir/
  fixtures: []
  steps: Start watcher. Move a previously-captured file from inbox/ into Projects/Alpha/subdir/.
    Query batch_id for the updated documents row.
  expected:
  - batch_id updated to the batch for Projects/Alpha/subdir. No re-capture triggered
    (summary and content_hash unchanged).
  spec_ref: docs/1_design/td040-td041-batch-id-fix.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestUpload::test_upload_new_path_returns_document_id
  status: active
  retired: null
- id: P2-REC-01
  behavior: reconcile strips deprecated frontmatter key from a note that already has
    valid tags and correct project field
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/note.md
  fixture_frontmatter: 'type: note

    tags: []

    project: Alpha

    domain: finance

    '
  steps:
  - 'Run reconcile:'
  - '`uv run kms reconcile`'
  - Open `Projects/Alpha/note.md` and inspect frontmatter.
  expected:
  - '`domain: finance` key absent from frontmatter'
  - '`project: Alpha` unchanged'
  - '`tags:` list unchanged (still empty)'
  spec_ref: docs/3_specs/td042-deprecated-key-strip-mini.md
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_strips_deprecated_key
  status: active
  retired: null
- id: P2-REC-02
  behavior: reconcile leaves a human-locked note untouched even if it has a deprecated
    frontmatter key
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/locked.md
  fixture_frontmatter: 'type: note

    tags: []

    project: Alpha

    domain: finance

    updated_by_human: true

    '
  steps:
  - 'Run reconcile:'
  - '`uv run kms reconcile`'
  - Open `Projects/Alpha/locked.md` and inspect frontmatter.
  expected:
  - '`domain: finance` still present in frontmatter (not stripped)'
  - '`updated_by_human: true` unchanged'
  - No other frontmatter fields modified
  spec_ref: docs/3_specs/td042-deprecated-key-strip-mini.md
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_leaves_deprecated_key_on_human_locked_note
  status: active
  retired: null
- id: P2-REC-03
  behavior: reconcile does not write a note that has no deprecated keys and no other
    dirty reason
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms reconcile
  fixtures:
  - Projects/Alpha/clean.md
  fixture_frontmatter: 'type: note

    tags: []

    project: Alpha

    '
  steps:
  - 'Run reconcile:'
  - '`uv run kms reconcile`'
  - Check the note's last-modified timestamp before and after — it should not change.
  expected:
  - Note frontmatter unchanged (no unexpected fields added or removed)
  - File modification time unchanged (reconcile did not rewrite it)
  spec_ref: docs/3_specs/td042-deprecated-key-strip-mini.md
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_reconcile.py::test_reconcile_stale_tags_does_not_write_note_without_deprecated_key
  status: active
  retired: null
- id: P2-REG-01
  behavior: Registry returns all Projects/<A>/ folders grouped under their declared
    domain
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: python -c 'from vault.registry import build_registry; ...' or internal
    pipeline call
  fixtures:
  - Projects/Alpha/CLAUDE.md
  - Projects/Beta/CLAUDE.md
  - Domain/Finance/CLAUDE.md
  fixture_frontmatter: 'tags:

    - domain/Finance

    '
  steps:
  - 'Set up Projects/Alpha/CLAUDE.md with tags: [domain/Finance] and Projects/Beta/CLAUDE.md
    with no domain tag.'
  - Ensure Domain/Finance/ folder exists.
  - Call build_registry(vault_cfg) and inspect the returned structure.
  expected:
  - Finance group contains Alpha (has matching Domain/Finance/ folder and domain/Finance
    tag)
  - Uncategorized group contains Beta (no domain tag)
  - Domain/Finance/ listed as a direct destination
  - Domain/Finance/Archive/ NOT included as a destination
  spec_ref: docs/1_design/project-registry.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-REG-02
  behavior: Project with stale domain tag (domain folder deleted) moves to Uncategorized
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: build_registry(vault_cfg) after domain folder removed
  fixtures:
  - Projects/Gamma/CLAUDE.md
  fixture_frontmatter: 'tags:

    - domain/OldDomain

    '
  steps:
  - 'Set up Projects/Gamma/CLAUDE.md with tags: [domain/OldDomain].'
  - Ensure Domain/OldDomain/ does NOT exist.
  - Call build_registry(vault_cfg) and inspect the Uncategorized group.
  expected:
  - Gamma appears in Uncategorized (not in any domain group)
  - No error raised — stale domain tag handled gracefully
  spec_ref: docs/1_design/project-registry.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-REG-03
  behavior: Project with no CLAUDE.md appears in Uncategorized
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: build_registry(vault_cfg) for project missing CLAUDE.md
  fixtures:
  - Projects/NoIndex/placeholder.md
  steps:
  - Create Projects/NoIndex/ folder with a file but NO CLAUDE.md.
  - Call build_registry(vault_cfg) and inspect the Uncategorized group.
  expected:
  - NoIndex appears in Uncategorized
  - No error raised — missing CLAUDE.md treated as no domain mapping
  spec_ref: docs/1_design/project-registry.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-REG-04
  behavior: Only the first domain/D tag is used when CLAUDE.md has multiple domain
    tags
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: build_registry(vault_cfg)
  fixtures:
  - Projects/Multi/CLAUDE.md
  fixture_frontmatter: 'tags:

    - domain/Finance

    - domain/Operations

    '
  steps:
  - Set up Projects/Multi/CLAUDE.md with two domain/ tags.
  - Ensure both Domain/Finance/ and Domain/Operations/ exist.
  - Call build_registry(vault_cfg) and check which group Multi appears in.
  expected:
  - 'Multi appears in exactly ONE group (the first domain tag: Finance)'
  - Multi does NOT appear in Operations group
  - Multi does NOT appear in Uncategorized
  spec_ref: docs/1_design/project-registry.md §Success criteria (ADR-0009 one-domain
    rule)
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-REG-05
  behavior: 'Watcher live-update: new project folder added to registry without restart'
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms watch + mkdir Projects/NewProject/ in vault
  fixtures: []
  steps:
  - Start kms watch.
  - Create Projects/NewProject/ directory in vault.
  - Wait for watcher to detect DirCreatedEvent (~3s).
  - Inspect registry state (log line or direct object check).
  expected:
  - Registry now includes NewProject in Uncategorized (no CLAUDE.md yet)
  - No kms watch restart required
  - Audit log entry written for registry update
  spec_ref: docs/1_design/project-registry.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-REG-06
  behavior: 'Watcher live-update: project CLAUDE.md created or updated changes domain
    grouping'
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms watch + write/update Projects/<A>/CLAUDE.md with domain tag
  fixtures:
  - Projects/Alpha/placeholder.md
  - Domain/Finance/placeholder.md
  steps:
  - Start kms watch with Projects/Alpha/ folder present but no CLAUDE.md.
  - Verify Alpha is in Uncategorized group.
  - 'Create Projects/Alpha/CLAUDE.md with tags: [domain/Finance].'
  - Wait for watcher to detect FileCreatedEvent (~3s).
  - Inspect registry state.
  expected:
  - Alpha moved from Uncategorized to Finance group in registry
  - No restart required
  spec_ref: docs/1_design/project-registry.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P2-CPROMPT-01
  behavior: Classify prompt returns valid JSON with target_type, target_name, confidence,
    and reasoning fields
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms classify <file> or direct Classify pipeline call with real LLM
  fixtures:
  - inbox/sample-note.md
  steps:
  - Place a note in inbox/ with title, summary, and tags already set.
  - Call the Classify pipeline with a valid valid_destinations string.
  - Inspect the raw LLM response.
  expected:
  - Response is valid JSON — no markdown fences, no prose
  - 'JSON contains exactly: target_type, target_name, confidence, reasoning'
  - target_type is either 'domain' or 'project'
  - confidence is a float between 0.0 and 1.0
  - reasoning is a non-empty string
  spec_ref: 'docs/2_specs/classify-prompt-mini.md §Done when #3'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: classify engine reshaped in P2-CIC Phase 3 — old target_type/target_name
    API replaced with project/domains/primary_domain. P2-CIC entries now cover classify
    behavior.'
- id: P2-CPROMPT-02
  behavior: Classify prompt routes active-work notes to a project and general-knowledge
    notes to a domain
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms classify <file> with two distinct note types and a vault with both
    project and domain destinations
  fixtures:
  - inbox/meeting-notes.md
  - inbox/industry-report.md
  - Projects/Alpha/CLAUDE.md
  - Domain/Finance/placeholder.md
  steps:
  - Classify a note whose summary describes active project work (e.g. meeting notes,
    deliverable).
  - Classify a note whose summary describes general reference material (e.g. industry
    report, background research).
  expected:
  - 'Active-work note: target_type=''project'''
  - 'General-knowledge note: target_type=''domain'''
  spec_ref: 'docs/2_specs/classify-prompt-mini.md §Done when #4'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: classify engine reshaped in P2-CIC Phase 3 — old target_type/target_name
    API replaced with project/domains/primary_domain. P2-CIC entries now cover classify
    behavior.'
- id: P2-CPROMPT-03
  behavior: Classify prompt never returns target_name='Uncategorized' — routes to
    actual project name within that group
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms classify <file> when valid_destinations contains an Uncategorized group
    with one or more projects
  fixtures:
  - inbox/sample-note.md
  steps:
  - Build a valid_destinations string that includes an Uncategorized group containing
    'OrphanProject'.
  - Classify a note that best matches OrphanProject.
  - Inspect target_name in the response.
  expected:
  - target_name is 'OrphanProject', not 'Uncategorized'
  - target_type is 'project'
  spec_ref: 'docs/2_specs/classify-prompt-mini.md §Done when #6'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: classify engine reshaped in P2-CIC Phase 3 — old target_type/target_name
    API replaced with project/domains/primary_domain. P2-CIC entries now cover classify
    behavior.'
- id: P2-CPROMPT-04
  behavior: Classify prompt returns low confidence rather than a forced answer when
    no destination fits well
  tier: smoke
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms classify <file> with a note whose content has no clear match in valid_destinations
  fixtures:
  - inbox/ambiguous-note.md
  steps:
  - Classify a note whose summary is vague or covers a topic absent from valid_destinations.
  - Inspect confidence in the response.
  expected:
  - confidence is below 0.7
  - target_name is still populated (best guess, not null)
  spec_ref: 'docs/2_specs/classify-prompt-mini.md §Done when #5'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: classify engine reshaped in P2-CIC Phase 3 — old target_type/target_name
    API replaced with project/domains/primary_domain. P2-CIC entries now cover classify
    behavior.'
- id: P2-CL-01
  behavior: classify() returns Success(ClassifyResult) with all four fields when AI
    returns valid JSON
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: classify() called with valid title, summary, tags, valid_destinations and
    AI returns valid JSON
  fixtures:
  - inbox/sample-note.md
  steps:
  - Call classify() with a note's title, summary, tags, and a formatted destinations
    string.
  - Mock the AI provider to return valid JSON with target_type, target_name, confidence,
    reasoning.
  - Inspect the return value.
  expected:
  - Returns Success(ClassifyResult)
  - ClassifyResult.target_type is 'project' or 'domain'
  - ClassifyResult.target_name is a non-empty string
  - ClassifyResult.confidence is a float between 0.0 and 1.0
  - ClassifyResult.reasoning is a non-empty string
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-01
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: ClassifyResult schema reshaped in P2-CIC Phase 3 — old target_type/target_name
    fields replaced with project/domains/primary_domain.'
- id: P2-CL-02
  behavior: classify() returns Failure(recoverable=True) when AI returns malformed
    JSON
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: classify() called and AI returns a response that is not valid JSON
  fixtures: []
  steps:
  - Call classify() with valid inputs.
  - Mock the AI provider to return a plain-text string (not JSON).
  - Inspect the return value.
  expected:
  - Returns Failure
  - Failure.recoverable is True
  - No exception raised
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-02
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify.py::test_bad_json_returns_retryable
  status: active
  retired: null
- id: P2-CL-03
  behavior: classify() returns Failure(recoverable=True) when JSON is valid but target_type
    is not 'project' or 'domain'
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: classify() called and AI returns JSON with an invalid target_type value
  fixtures: []
  steps:
  - Call classify() with valid inputs.
  - Mock the AI provider to return JSON where target_type is 'folder' or missing.
  - Inspect the return value.
  expected:
  - Returns Failure
  - Failure.recoverable is True
  - No exception raised
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-03
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: retired
  retired: '2026-06-09: ClassifyResult schema reshaped in P2-CIC Phase 3 — old target_type/target_name
    fields replaced with project/domains/primary_domain.'
- id: P2-CL-04
  behavior: classify() never raises an exception and never returns None
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: classify() called under any failure condition (AI failure, bad JSON, network
    error)
  fixtures: []
  steps:
  - Call classify() with valid inputs.
  - Mock the AI provider to raise an exception.
  - Inspect the return value.
  expected:
  - Returns Failure — no exception propagates to the caller
  - Return value is never None
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-04
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify.py::test_never_raises
  status: active
  retired: null
- id: P2-CL-05
  behavior: classify() loads prompt from prompts/classify.yaml — no prompt string
    in Python code
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: 'Code review: inspect pipelines/classify.py for any hardcoded prompt strings'
  fixtures: []
  steps:
  - Read src/pipelines/classify.py (or wherever classify() lives).
  - Search for any string literals that look like AI instructions.
  expected:
  - No prompt string found in Python source
  - Prompt is loaded via PROMPTS['classify'] from prompt_loader
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-05
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify.py::test_no_prompt_in_source
  status: active
  retired: null
- id: P2-CL-06
  behavior: classify() calls the AI via llm/provider.py get_provider() — no direct
    AI library import
  tier: unit
  phase: 2
  origin: design
  granularity: outcome
  trigger: 'Code review: inspect classify() implementation for direct anthropic or
    openai imports'
  fixtures: []
  steps:
  - Read src/pipelines/classify.py (or wherever classify() lives).
  - Check imports for anthropic, openai, or other AI library direct calls.
  expected:
  - No direct AI library import in classify() module
  - AI call goes through get_provider() from llm/provider.py
  spec_ref: docs/1_design/phase2/classify.md §P2-CL-06
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify.py::test_no_direct_ai_import
  status: active
  retired: null
- id: P2-CIC-01
  behavior: Loose inbox .md note with a confident destination is moved into the project/domain
    folder DERIVED from its assigned tags+project (AUTO)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-01-clear-project-note.md
  steps:
  - Place a note in inbox/ whose content clearly belongs to an existing project (e.g.
    meeting notes for Projects/Alpha).
  - Ensure Projects/Alpha/ exists with a CLAUDE.md domain tag.
  - '`uv run kms capture inbox/p2cic-01-clear-project-note.md`'
  expected:
  - Note is GONE from inbox/ (moved to the DERIVED folder, e.g. Projects/Alpha/p2cic-01-clear-project-note.md)
  - 'Frontmatter has project: Alpha AND the destination folder matches that project
    field (location derived from the field, not picked independently)'
  - 'No status: needs-review field (AUTO acted, not suggested)'
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Routing model
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_md_moves_to_project_folder
  status: active
  retired: null
- id: P2-CIC-02
  behavior: Loose inbox note with a medium-confidence destination stays in inbox with
    suggested destination recorded (SUGGEST)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-02-ambiguous-note.md
  steps:
  - Place a note in inbox/ whose content plausibly fits a destination but is not clear-cut.
  - '`uv run kms capture inbox/p2cic-02-ambiguous-note.md`'
  expected:
  - Note STAYS at inbox/p2cic-02-ambiguous-note.md (not moved)
  - 'Frontmatter records a suggested destination (e.g. status: needs-review plus a
    suggested-destination field)'
  - Frontmatter records the AI confidence and one-sentence reasoning
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_inbox_suggest
  status: active
  retired: null
- id: P2-CIC-03
  behavior: Loose inbox note the AI cannot place stays in inbox marked stuck (CLUELESS)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-03-no-match-note.md
  steps:
  - Place a note in inbox/ whose topic matches no existing project or domain.
  - '`uv run kms capture inbox/p2cic-03-no-match-note.md`'
  expected:
  - Note STAYS at inbox/p2cic-03-no-match-note.md (not moved)
  - Frontmatter records that the AI was stuck (low/no candidate) with its reasoning
  - No destination move occurred
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_inbox_clueless
  status: active
  retired: null
- id: P2-CIC-04
  behavior: A file dropped directly into a project/domain folder is filed by location
    WITHOUT any AI classify call
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <Projects/Alpha/note.md>
  fixtures:
  - Projects/Alpha/p2cic-04-located-note.md
  steps:
  - Place a note directly inside Projects/Alpha/.
  - '`uv run kms capture Projects/Alpha/p2cic-04-located-note.md`'
  expected:
  - Note stays in Projects/Alpha/ (location wins; no move)
  - 'Frontmatter has project: Alpha'
  - No suggested-destination / needs-review fields written (classify was skipped)
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Success criteria (LOCATED
    symmetry)
  human_reviewed: 'yes'
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_located_project_passthrough
  status: active
  retired: null
- id: P2-CIC-05
  behavior: Loose inbox PDF gets a rich attachment summary AND is classified at drop
    time (two AI calls; pending-routing marker retired)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/report.pdf>
  fixtures:
  - inbox/p2cic-05-clear-project-report.pdf
  steps:
  - Place a PDF in inbox/ whose content clearly belongs to an existing project.
  - '`uv run kms capture inbox/p2cic-05-clear-project-report.pdf`'
  expected:
  - Sibling .md has a real summary (NOT the one-line pending-routing placeholder)
  - 'On AUTO: binary moved to the chosen folder''s attachment/, sibling moved alongside,
    both findable'
  - 'On SUGGEST/CLUELESS: binary stays in inbox; sibling records the suggested destination
    or stuck state'
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_binary_end_to_end_placement
  status: active
  retired: null
- id: P2-CIC-06
  behavior: Every classify outcome (AUTO/SUGGEST/CLUELESS) writes exactly one decision-log
    audit row
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-06-audit-note.md
  steps:
  - '`uv run kms capture inbox/p2cic-06-audit-note.md`'
  - '`sqlite3 data/kb.db "SELECT pipeline, stage, outcome, reasoning, source_ids FROM
    audit_log WHERE stage=''classify'' ORDER BY rowid DESC LIMIT 3"`'
  expected:
  - An audit_log row exists with pipeline=capture, stage=classify
  - outcome is one of AUTO | SUGGEST | CLUELESS
  - reasoning is non-empty; source_ids contains the note's vault path
  spec_ref: CONSTRAINTS.md C-13 'audit log non-negotiable'
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_human_locked_fallback
  status: active
  retired: null
- id: P2-CIC-07
  behavior: A classify AUTO-move to a project/domain root leaves batch_id NULL; index
    location stays consistent
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms capture <inbox/note.md> (AUTO routes to Projects/Alpha root)
  fixtures:
  - inbox/p2cic-07-batch-note.md
  steps:
  - Capture a loose inbox note that AUTO-routes to a project ROOT (e.g. Projects/Alpha/).
  - '`sqlite3 data/kb.db "SELECT vault_path, batch_id FROM documents WHERE vault_path
    LIKE ''%p2cic-07-batch-note%''"`'
  expected:
  - documents.vault_path equals the new in-folder path (e.g. Projects/Alpha/p2cic-07-batch-note.md)
    — matches disk
  - batch_id is NULL (a tree root is not a batch-worthy subfolder)
  - Exactly one documents row exists for the note (no orphan row at the old inbox
    path)
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Risks R2/R3
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_binary_batch_id_null
  status: active
  retired: null
- id: P2-CIC-08
  behavior: A classify AUTO-move under the running watcher does not double-fire re-home
    or create a duplicate row
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms watch + a loose inbox file that classify AUTO-routes
  fixtures: []
  steps:
  - Start kms watch.
  - Drop a loose inbox file whose content clearly belongs to a project, wait for capture
    to AUTO-route it.
  - '`sqlite3 data/kb.db "SELECT count(*), vault_path FROM documents WHERE vault_path
    LIKE ''%<that-file>%'' GROUP BY vault_path"`'
  expected:
  - Exactly one documents row, pointing at the new destination path
  - Watcher re-home was suppressed for the pipeline-initiated move (log shows rehome_skip
    reason=pipeline_initiated)
  - No second move back to inbox; the file stays at its classified destination
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Risks R1/R4
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_md_move_guard_registered
  status: active
  retired: null
- id: P2-CIC-09
  behavior: A file captured as part of a dropped folder is fully captured but NOT
    individually re-classified (SUPPRESS — the folder is the routing unit)
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms watch / capture_folder on a folder dropped in inbox/
  fixtures: []
  steps:
  - Drop a folder containing 2-3 loose files into inbox/ (a folder the system routes
    or marks CLUELESS).
  - Let the folder capture flow process it (watcher stable-folder event, or capture_folder).
  - '`sqlite3 data/kb.db "SELECT count(*) FROM audit_log WHERE stage=''classify''"`
    and inspect each file''s frontmatter.'
  expected:
  - Each file inside the folder is summarized + frontmatter-stamped + batch-stamped
    (full capture ran)
  - 'NO file inside the folder carries suggested_type / suggested_name / classify_confidence
    / status: needs-review from an individual classify call'
  - No per-file stage='classify' audit row is written for the folder's files (the
    folder verdict is the only classify decision)
  spec_ref: docs/2_specs/phase2/classify-inline-capture.md §Component 3 (SUPPRESS);
    locked Decision 3
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_suppress_passthrough
  status: active
  retired: null
- id: P2-CIC-10
  behavior: A single file dropped in a project/domain folder is still summarized +
    frontmatter-stamped; only the classify step and the move are skipped (LOCATED
    is NOT a no-op)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <Projects/Alpha/note.md>
  fixtures:
  - Projects/Alpha/p2cic-10-located-summary-note.md
  steps:
  - Place a note directly inside Projects/Alpha/ with body content worth summarizing.
  - '`uv run kms capture Projects/Alpha/p2cic-10-located-summary-note.md`'
  expected:
  - Note stays in Projects/Alpha/ (no move)
  - 'Frontmatter has a real AI summary and project: Alpha (earlier capture stages
    ran)'
  - No suggested-destination / classify_confidence / needs-review fields (classify
    step was skipped)
  - No stage='classify' audit row for this file (LOCATED — no classify AI call)
  spec_ref: docs/2_specs/phase2/classify-inline-capture.md §Feature overview (LOCATED);
    locked Decision 4
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_located_project_passthrough
  status: active
  retired: null
- id: P2-CIC-11
  behavior: When the AI assigns a project, the note moves to that Project folder even
    though it also carries domain tags (project beats domain — precedence)
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-11-project-and-domain-note.md
  steps:
  - Place a loose inbox note that clearly belongs to an existing project AND carries
    one or more domain tags (e.g. a deliverable for Projects/Alpha that is also about
    domain/Finance).
  - Ensure both Projects/Alpha/ and Domain/Finance/ exist.
  - '`uv run kms capture inbox/p2cic-11-project-and-domain-note.md`'
  expected:
  - Note moves to Projects/Alpha/ (the Project root), NOT Domain/Finance/ — project
    field wins over the domain tag
  - Frontmatter still carries the domain/Finance tag (tags are not stripped; only
    the move follows precedence)
  - 'Frontmatter project: Alpha'
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Routing model (precedence)
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_project_beats_domain
  status: active
  retired: null
- id: P2-CIC-12
  behavior: A loose note with NO project and MULTIPLE domain tags moves to the AI's
    designated PRIMARY domain folder while keeping all its domain tags
  tier: phase
  phase: 2
  origin: design
  granularity: outcome
  trigger: kms capture <inbox/note.md>
  fixtures:
  - inbox/p2cic-12-multi-domain-note.md
  steps:
  - Place a loose inbox note that spans two existing domains (e.g. tagged domain/Finance
    and domain/Legal) and ties to no specific project.
  - Ensure Domain/Finance/ and Domain/Legal/ both exist.
  - '`uv run kms capture inbox/p2cic-12-multi-domain-note.md`'
  expected:
  - Note moves to exactly ONE domain folder — the AI's designated primary (e.g. Domain/Finance/),
    not both
  - Frontmatter still lists BOTH domain tags (domain/Finance AND domain/Legal) — only
    the move uses the primary
  - Frontmatter records no project (no project field set)
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Routing model (primary
    domain)
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_classify_step_multi_domain_primary_routing
  status: active
  retired: null
- id: P2-CIC-13
  behavior: 'Structural consistency: an AUTO-filed note''s on-disk folder always matches
    its stamped project/primary-domain (no free pick that could disagree)'
  tier: full
  phase: 2
  origin: design
  granularity: mechanism
  trigger: kms capture <inbox/note.md> (any AUTO route)
  fixtures:
  - inbox/p2cic-13-consistency-note.md
  steps:
  - Capture any loose inbox note that AUTO-routes.
  - '`sqlite3 data/kb.db "SELECT vault_path, project FROM documents WHERE vault_path
    LIKE ''%p2cic-13-consistency-note%''"` and read the note''s frontmatter.'
  - Compare the folder portion of vault_path against the stamped project (or primary
    domain) field.
  expected:
  - If a project is stamped, vault_path is under Projects/<that project>/
  - If no project but a primary domain, vault_path is under Domain/<that primary domain>/
  - The folder NEVER points at a project/domain the frontmatter does not name (derived,
    not free pick)
  spec_ref: docs/1_design/phase2/classify-inline-capture.md §Routing model (structural
    consistency)
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_pipelines/test_classify_step.py::test_auto_md_replace_path_atomic
  status: active
  retired: null
- id: P3-IDX-01
  behavior: After capture, an embeddings_vec row exists for the note (meaning index
    populated)
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: kms capture <file>
  fixtures: []
  steps:
  - Capture a .md note.
  - Query embeddings_vec for the note's vault_path.
  expected:
  - One row exists in embeddings_vec for that vault_path
  - The embedding blob is non-null (384 floats, 1536 bytes)
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_search.py::test_capture_md_creates_embedding_row
  status: active
  retired: null
- id: P3-IDX-02
  behavior: After capture, a keyword in the note body is findable via FTS5 MATCH
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: kms capture <file>
  fixtures: []
  steps:
  - Capture a .md note containing a distinctive keyword.
  - Query notes_fts with MATCH on that keyword.
  expected:
  - One row exists in notes_fts for that vault_path
  - FTS5 MATCH returns the note
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_search.py::test_capture_md_creates_fts_row
  status: active
  retired: null
- id: P3-IDX-03
  behavior: Embedding index failure does not block capture
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: kms capture <file> (with mocked embedding failure)
  fixtures: []
  steps:
  - Mock index_embedding to raise.
  - Capture a note.
  expected:
  - Capture returns Success
  - No embedding row exists (failure was caught and logged)
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_search.py::test_capture_embedding_failure_nonfatal
  status: active
  retired: null
- id: P3-IDX-04
  behavior: Keyword index failure does not block capture
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: kms capture <file> (with mocked keyword failure)
  fixtures: []
  steps:
  - Mock index_keywords to raise.
  - Capture a note.
  expected:
  - Capture returns Success
  - No FTS5 row exists (failure was caught and logged)
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_search.py::test_capture_keyword_failure_nonfatal
  status: active
  retired: null
- id: P3-IDX-05
  behavior: Deleting a note removes its search entries from both embeddings_vec and
    notes_fts in the same transaction
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: documents.delete_by_path(vault_path)
  fixtures: []
  steps:
  - Index a note in both search tables.
  - Call delete_by_path.
  expected:
  - Zero rows in embeddings_vec for that vault_path
  - Zero rows in notes_fts for that vault_path
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_storage/test_documents_search.py::test_delete_cleans_search_tables
  status: active
  retired: null
- id: P3-IDX-06
  behavior: Renaming a note copies its search entries to the new path and removes
    the old ones atomically
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: documents.rename(old_path, new_path)
  fixtures: []
  steps:
  - Index a note in both search tables at path A.
  - Call rename(A, B).
  expected:
  - Zero search rows at path A
  - One row each in embeddings_vec and notes_fts at path B
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_storage/test_documents_search.py::test_rename_copies_search_entries
  status: active
  retired: null
- id: P3-IDX-07
  behavior: The embedding contextual string follows a deterministic format
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: index_embedding() called
  fixtures: []
  steps:
  - Call index_embedding with known title, type, tags, summary.
  expected:
  - String format includes title, type, tags, summary in order
  - Summary omitted when None/empty
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_embeddings.py::test_index_embedding_context_string_format
  status: active
  retired: null
- id: P3-IDX-08
  behavior: Every database connection loads sqlite-vec extension automatically
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: get_connection() called
  fixtures: []
  steps:
  - Open a connection via get_connection().
  - Run SELECT vec_version().
  expected:
  - Query succeeds without error
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_storage/test_db.py::test_vec_extension_loaded
  status: active
  retired: null
- id: P3-IDX-09
  behavior: Migration 007 creates both search tables idempotently
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: init_db() called twice
  fixtures: []
  steps:
  - Run init_db() on a fresh database, then again.
  expected:
  - embeddings_vec and notes_fts virtual tables exist
  - schema_version = 7
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_storage/test_migration_007.py::test_migration_007_tables_exist
  status: active
  retired: null
- id: P3-IDX-10
  behavior: SearchConfig loads with defaults for all four fields
  tier: unit
  phase: 3
  origin: specification
  granularity: mechanism
  trigger: CONFIG.main.search accessed
  fixtures: []
  steps:
  - Load config with no search section, access CONFIG.main.search.
  expected:
  - embedding_model = "all-MiniLM-L6-v2"
  - reranker_model = "cross-encoder/ms-marco-MiniLM-L-6-v2"
  - max_candidates = 50 and max_results = 20
  spec_ref: docs/2_specs/p3_session_a_index_layer.md
  human_reviewed: false
  last_tested: '2026-06-10'
  last_result: passed
  pytest_ref: tests/test_core/test_config.py::TestSearchConfig::test_search_config_defaults
  status: active
  retired: null
- id: P3-SRCH-01
  behavior: A question finds a semantically related note even when the wording differs
    (vector match, not just keywords)
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search "<query>"
  fixtures: []
  steps:
  - Capture a note whose summary/body describes "managing pushback in meetings".
  - 'Run: uv run kms search "stakeholder resistance"'
  expected:
  - The pushback note appears in the result list
  - Each result block shows the note's real title, a score, and a snippet
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_ranker.py::test_rank_returns_semantically_relevant_note_first
  status: active
  retired: null
- id: P3-SRCH-02
  behavior: A project filter with no question returns every note in that project,
    newest first (filter-only mode)
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search --project <Name>
  fixtures: []
  steps:
  - Capture several notes into Projects/Alpha/ over time.
  - 'Run: uv run kms search --project Alpha'
  expected:
  - All Alpha notes are returned with no query term required
  - Results are ordered most-recently-updated first
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_storage/test_documents_filter.py::test_filter_by_project_returns_only_matching
  status: active
  retired: null
- id: P3-SRCH-03
  behavior: A question plus a project filter searches semantically but only within
    that project
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search "<query>" --project <Name>
  fixtures: []
  steps:
  - Capture notes into two projects, both touching the query topic.
  - 'Run: uv run kms search "budget Q3" --project Alpha'
  expected:
  - Only Alpha notes appear in the results
  - Results are ranked by relevance to the question
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_search.py::test_search_query_plus_project_scopes_ranking
  status: active
  retired: null
- id: P3-SRCH-04
  behavior: Every search result carries a triage payload (handle, summary, snippet,
    score, metadata) and never the full note body
  tier: full
  phase: 3
  origin: design
  granularity: mechanism
  trigger: kms search "<query>"
  fixtures: []
  steps:
  - Run any query that returns at least one result.
  expected:
  - Each result has vault_path, summary, snippet, score, and metadata
  - No result contains the full note body
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_reranker.py::test_rerank_returns_search_results_with_metadata
  status: active
  retired: null
- id: P3-SRCH-05
  behavior: A result for a binary's sibling summary shows a usable title, not the
    raw "report.pdf.md" filename
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search "<query>"
  fixtures: []
  steps:
  - Capture a PDF/binary so its sibling summary note is indexed.
  - Run a query that matches the binary's content.
  expected:
  - The result's title is the human-readable note title from the index
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_reranker.py::test_rerank_title_is_descriptive
  status: active
  retired: null
- id: P3-SRCH-06
  behavior: Search skips index rows whose underlying note was deleted instead of crashing
  tier: full
  phase: 3
  origin: design
  granularity: mechanism
  trigger: kms search "<query>"
  fixtures: []
  steps:
  - Leave an index entry whose documents row was removed.
  - Run a query that would otherwise return that entry.
  expected:
  - Search returns the other valid results and silently skips the stale entry
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_reranker.py::test_rerank_skips_stale_row
  status: active
  retired: null
- id: P3-SRCH-07
  behavior: Rebuilding both indexes is idempotent — running reindex twice yields identical
    results
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search --reindex
  fixtures: []
  steps:
  - 'Run: uv run kms search --reindex'
  - Run the same reindex a second time.
  expected:
  - The reindex reports a count of notes processed
  - Second run produces no duplicate index rows
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_cli/test_search_command.py::test_search_reindex_flag
  status: active
  retired: null
- id: P3-SRCH-08
  behavior: A date-range filter with no question returns recent notes (supports a
    future weekly synthesis caller)
  tier: full
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms search --since <window>
  fixtures: []
  steps:
  - Capture notes across several days.
  - 'Run: uv run kms search --since 7d'
  expected:
  - Only notes updated within the window are returned, newest first
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_retrieval/test_search.py::test_search_date_range_filters
  status: active
  retired: null
- id: P3-SRCH-09
  behavior: Classify no longer accepts a domain name as a valid project destination
    (cross-type leak closed, TD-051)
  tier: full
  phase: 3
  origin: design
  granularity: mechanism
  trigger: classify() with a domain name supplied as project
  fixtures: []
  steps:
  - Drive classify() so the AI response puts a domain name in the project field.
  expected:
  - classify() returns Failure(recoverable=True)
  - project validated only against project names, primary_domain only against domain
    names
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_pipelines/test_classify.py::test_classify_rejects_domain_as_project
  status: active
  retired: null
- id: P3-SRCH-10
  behavior: A captured binary's sibling note carries an AI-generated descriptive title
    in frontmatter
  tier: phase
  phase: 3
  origin: design
  granularity: outcome
  trigger: kms capture <Projects/Alpha/report.pdf>
  fixtures: []
  steps:
  - Capture a PDF/binary so its sibling summary note is written and indexed.
  - Open the sibling .md and inspect its frontmatter.
  expected:
  - Sibling frontmatter carries a descriptive title field (the AI's title)
  - The documents.title for the sibling row is the descriptive title
  spec_ref: docs/1_design/P3_session_b_query_path.md
  human_reviewed: false
  last_tested: '2026-06-11'
  last_result: passed
  pytest_ref: tests/test_vault/test_frontmatter.py::test_title_field_roundtrip
  status: active
  retired: null
- id: P4-MCP-01
  behavior: Claude Desktop connects to the KMS MCP server over stdio and lists the
    five MVP tools
  tier: smoke
  phase: 4
  origin: design
  granularity: outcome
  trigger: claude desktop launches the configured stdio server entry point
  fixtures: []
  steps:
  - Add the stdio server entry to claude_desktop_config.json and restart Claude Desktop.
  - Open a chat and check the available KMS tools.
  expected:
  - 'Five tools are listed: kms_vault_info, kms_search, kms_read, kms_inspect, kms_move'
  - No tool returns a connection error on a no-op call
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-02
  behavior: kms_vault_info returns the project list, domain list, inbox count, last
    capture time, and vault-root context
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_vault_info (no arguments)
  fixtures: []
  steps:
  - From chat, ask the AI to discover the vault structure.
  expected:
  - Response lists active project names and domain names taken from the live Project
    Registry (NOT a meta.yaml file)
  - Response includes an inbox note count and a last-capture timestamp
  - If a vault-root CLAUDE.md exists, its content is included once (hash-deduped thereafter)
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-03
  behavior: kms_search returns context blocks before result cards for a domain/project-concentrated
    query
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_search(query="...", project="...") on a focused query
  fixtures: []
  steps:
  - Ask "what do I know about <a project that owns most matching notes>?"
  expected:
  - The response places the matching domain/project CLAUDE.md (and domain context.yaml
    if present) BEFORE the result cards
  - Each result card carries vault_path, title, summary, snippet, score, project,
    note_type
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-04
  behavior: A broad query that concentrates no single domain/project injects zero
    context files
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_search on a broad cross-cutting query
  fixtures: []
  steps:
  - Ask "what happened this week?" against a vault whose recent notes span many projects.
  expected:
  - No domain/project reaches the configured frequency threshold
  - The response contains result cards only, no context blocks
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-05
  behavior: kms_read returns minority-domain context then full note content for a
    batch of paths
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_read(paths=[...]) after a search
  fixtures: []
  steps:
  - From the search cards, read several notes including one from a domain whose context
    was not yet sent.
  expected:
  - Context for the not-yet-sent domain/project appears BEFORE the full note content
  - Each note returns metadata as a dict plus the body as markdown text
  - For a binary-backed sibling note, the body is the AI summary (not the binary bytes)
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-06
  behavior: kms_inspect re-extracts raw text from a binary-backed note's original
    file
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_inspect(path=<sibling .md path or binary path>)
  fixtures: []
  steps:
  - Inspect a search result whose note_type is attachment-summary.
  expected:
  - The server resolves the sibling to its binary via the attachment_path frontmatter
    field
  - The response returns the raw extracted text from the binary (no new AI call)
  - Passing the binary path directly yields the same result
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-07
  behavior: kms_move relocates a CLUELESS inbox note to a named project/domain and
    updates its frontmatter + index
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_move(path=<inbox note>, destination=<project or domain name>)
  fixtures: []
  steps:
  - Read a CLUELESS inbox note's classify_reasoning, then ask the AI to move it to
    a named project.
  expected:
  - The note file moves into the named project/domain folder
  - Its frontmatter project / primary-domain reflects the destination
  - The documents index path is updated (old search rows cleared) and the watcher
    does not re-home it
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-08
  behavior: A second search on an already-seen domain replaces full context with a
    short already-provided note
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_search twice on the same domain within one conversation
  fixtures: []
  steps:
  - Run a focused search, then run a second focused search on the same domain in the
    same chat.
  expected:
  - The first response carries the full domain context
  - The second response replaces it with a short "context for X already provided"
    note
  - A new conversation (new process) sends the full context again
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P4-MCP-09
  behavior: include_context=true forces full context re-injection regardless of dedup
    state
  tier: phase
  phase: 4
  origin: design
  granularity: outcome
  trigger: AI calls kms_search or kms_read with include_context=true after context
    was already sent
  fixtures: []
  steps:
  - After context for a domain was already sent and deduped, call again with include_context=true.
  expected:
  - The full context content is re-injected even though its hash was already seen
  spec_ref: docs/1_design/P4_mcp_context_injection.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P5-DATA-01
  behavior: The new knowledge_entries table exists after the database is initialized,
    with all eleven columns
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: init_db() runs (migration 008 applied)
  fixtures: []
  steps:
  - Initialize a fresh database (run any command that calls init_db, or init_db directly
    against a temp db path).
  - 'Inspect the schema: `sqlite3 <db> "PRAGMA table_info(knowledge_entries)"`'
  - 'Check the version: `sqlite3 <db> "SELECT version FROM schema_version"`'
  expected:
  - knowledge_entries table exists with columns id, dimension, entity, tag, fact,
    status, confidence, sources, reasoning, created_at, updated_at
  - schema_version is 8
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_migration_008.py::test_migration_008_creates_knowledge_entries_table
  status: active
  retired: null
- id: P5-DATA-02
  behavior: The documents table gains three new nullable columns and every existing
    row reads back with them NULL (no breakage)
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: init_db() runs on a database that already has documents rows
  fixtures: []
  steps:
  - Initialize a database that already contains captured documents rows (migration
    008 applied on top).
  - 'Inspect the schema: `sqlite3 <db> "PRAGMA table_info(documents)"`'
  - 'Read an existing row: `sqlite3 <db> "SELECT full_body, original_filename, file_size_bytes
    FROM documents LIMIT 1"`'
  expected:
  - documents has the new columns full_body (TEXT), original_filename (TEXT), file_size_bytes
    (INTEGER), all nullable
  - The three new columns read back as NULL on every pre-existing row (nothing populates
    them in Slice 1)
  - get_by_path returns a DocumentRow without error for an existing row
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_migration_008.py::test_migration_008_document_columns_added
  status: active
  retired: null
- id: P5-DATA-03
  behavior: A knowledge entry can be created and read back by dimension, carrying
    its fact, status, confidence, and source list
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: knowledge_entries.upsert(...) then knowledge_entries.query_by_dimension(...)
  fixtures: []
  steps:
  - Call upsert() to store an entry (e.g. dimension=people, entity=Anthony, tag=role,
    fact="Product Lead for Movie Q2", status=confident, a confidence score, sources=[a
    document reference]).
  - Call query_by_dimension("people") and inspect the returned rows.
  expected:
  - upsert returns Success with the new row id
  - query_by_dimension returns the entry with its fact, status, confidence, and source
    list intact (sources round-trips as a list)
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_knowledge_entries.py::test_upsert_and_query_by_dimension
  status: active
  retired: null
- id: P5-DATA-04
  behavior: Querying by entity returns only that entity's entries across its tags
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: knowledge_entries.query_by_entity(...)
  fixtures: []
  steps:
  - Store two entries for one entity under different tags, plus one entry for a different
    entity.
  - Call query_by_entity for the first entity.
  expected:
  - Only the two entries for that entity are returned; the other entity's entry is
    excluded
  - Each returned entry carries its own tag and fact
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_knowledge_entries.py::test_query_by_entity_returns_only_that_entity
  status: active
  retired: null
- id: P5-DATA-05
  behavior: Retiring an entry flips its status to retired with a reason and never
    deletes the row
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: knowledge_entries.retire(entry_id, reason)
  fixtures: []
  steps:
  - Store a confident entry, then call retire() on it with a reason string.
  - Read the row back by its id.
  expected:
  - The row still exists (not deleted)
  - status is now retired and reasoning records the supplied reason
  - updated_at is refreshed
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_knowledge_entries.py::test_retire_keeps_row_flips_status
  status: active
  retired: null
- id: P5-DATA-06
  behavior: get_confident_and_pending returns confident and pending entries but excludes
    retired ones (the set fed to the extraction prompt)
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: knowledge_entries.get_confident_and_pending(...)
  fixtures: []
  steps:
  - Store three entries for one entity — one confident, one pending, one retired.
  - Call get_confident_and_pending for that entity (or dimension).
  expected:
  - The confident and pending entries are returned
  - The retired entry is NOT returned (retired entries are excluded from extraction
    input)
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_knowledge_entries.py::test_get_confident_and_pending_excludes_retired
  status: active
  retired: null
- id: P5-DATA-07
  behavior: A valid dimension/tag pair is accepted and an invented one is rejected
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: validate_dimension_tag(dimension, tag, config)
  fixtures: []
  steps:
  - Load the dimension/tag config.
  - Call validate_dimension_tag("people", "role", config) and validate_dimension_tag("people",
    "invented_tag", config).
  expected:
  - The valid pair returns Success
  - The invalid tag returns Failure (unknown tag for that dimension)
  - An unknown dimension also returns Failure
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_core/test_dimensions.py::TestDimensionValidation::test_known_dimension_known_tag_returns_success
  status: active
  retired: null
- id: P5-DATA-08
  behavior: Every dimension in the config carries a mandatory "other" catch-all tag
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: validate_dimension_tag(any_dimension, "other", config)
  fixtures: []
  steps:
  - Load the dimension/tag config.
  - For each configured dimension, call validate_dimension_tag(dimension, "other",
    config).
  expected:
  - The tag "other" validates as a known tag for every dimension (catch-all present
    everywhere)
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_core/test_dimensions.py::TestDimensionValidation::test_catch_all_other_tag_returns_success_for_every_dimension
  status: active
  retired: null
- id: P5-DATA-09
  behavior: A confidence score maps to a confident/pending status using thresholds
    read from config, not hardcoded floats
  tier: full
  phase: 5
  origin: design
  granularity: mechanism
  trigger: status-from-confidence mapping used by knowledge_entries.upsert / classify
  fixtures: []
  steps:
  - Feed a high confidence score (above the configured confident threshold) and a
    low one (below it) through the status mapping.
  - 'Inspect the source: confirm the threshold comes from config, not a literal in
    code.'
  expected:
  - High score maps to status confident; low score maps to status pending
  - The threshold value is read from config (no float literal in an if/elif drives
    the mapping)
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_core/test_dimensions.py::TestConfidenceToStatus::test_high_score_maps_to_confident
  status: active
  retired: null
- id: P5-DATA-10
  behavior: The full existing test suite stays green after the additive migration
    and new modules land, except the expected migration version-pin bump
  tier: full
  phase: 5
  origin: design
  granularity: mechanism
  trigger: uv run pytest
  fixtures: []
  steps:
  - 'Run the full suite: `uv run pytest`'
  expected:
  - All previously-passing tests still pass, EXCEPT the two version-pin assertions
    in tests/test_storage/test_migration_007.py (lines 41, 56) which are updated 7->8
    as the expected migration-version bump (research A1, 2026-06-12) — not a regression
  - No other existing test is rewritten or deleted to accommodate Slice 1
  - documents.upsert() still accepts a WriteOutcome (signature unchanged)
  spec_ref: docs/1_design/P5_slice1_data_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_migration_007.py
  status: active
  retired: null
- id: P5-DEPLOY-01
  behavior: The container image builds for the cloud target platform and the container
    starts and reports itself alive on the one shared port
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: docker build --platform linux/amd64 . ; docker run -p 8080:8080 <image>
  fixtures: []
  steps:
  - Build the image targeting linux/amd64.
  - Run the container mapping port 8080.
  - 'Hit the health path: `curl -s http://localhost:8080/health`'
  expected:
  - The image build completes without error
  - The container starts and stays up
  - GET /health returns HTTP 200 with a small ok body (e.g. {"status":"ok"})
  - /health needs no secret key (it is open)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_cloud_entry.py::test_health_returns_200_without_key
  status: active
  retired: null
- id: P5-DEPLOY-02
  behavior: The existing knowledge-assistant (MCP) interface answers a tool-list request
    on the same single port, served from the HTTP entry point
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: HTTP MCP request to the running container on port 8080
  fixtures: []
  steps:
  - Start the container.
  - Send an MCP tools/list request to the MCP path on port 8080.
  expected:
  - The MCP server responds with the registered tool list (the five existing KMS tools)
  - It is reachable on the same port as /health and /api/* (one port, different paths)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_upsert_from_upload.py::test_different_hash_updates_in_place
  status: active
  retired: null
- id: P5-DEPLOY-03
  behavior: A first upload of a new file stores a document record carrying its full
    extracted text and returns the new record id
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/upload with a valid secret key and a new vault_path payload
  fixtures: []
  steps:
  - 'POST a payload to /api/upload with the correct Authorization bearer key: a new
    vault_path, extracted_text, a content_hash, original_filename, file_size_bytes,
    and a metadata object.'
  - 'Read the stored row: `sqlite3 <db> "SELECT full_body, original_filename, file_size_bytes,
    content_hash FROM documents WHERE vault_path = ?"`'
  expected:
  - The response is HTTP 200 with a document id
  - A documents row exists for that vault_path with full_body equal to the uploaded
    extracted text, original_filename, file_size_bytes, and content_hash set
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestUpload::test_upload_new_path_returns_document_id
  status: active
  retired: null
- id: P5-DEPLOY-04
  behavior: Re-uploading the identical file (same path, same content fingerprint)
    is idempotent — the stored record is not duplicated or rewritten
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/upload twice with the same vault_path and the same content_hash
  fixtures: []
  steps:
  - Upload a file once (as in P5-DEPLOY-03).
  - Upload the exact same payload again.
  - 'Count rows: `sqlite3 <db> "SELECT COUNT(*) FROM documents WHERE vault_path =
    ?"`'
  expected:
  - Both requests return HTTP 200
  - There is exactly one documents row for that vault_path (no duplicate)
  - The second upload makes no content change (skip-on-same-hash)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_upsert_from_upload.py::test_same_hash_skips_write
  status: active
  retired: null
- id: P5-DEPLOY-05
  behavior: Uploading the same path with a different content fingerprint updates the
    stored text and details in place
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/upload with an existing vault_path but a new content_hash
  fixtures: []
  steps:
  - Upload a file once.
  - Upload the same vault_path again with changed extracted_text and a different content_hash.
  - 'Read the row back: `sqlite3 <db> "SELECT full_body, content_hash FROM documents
    WHERE vault_path = ?"`'
  expected:
  - The single documents row for that vault_path now holds the new extracted text
    and the new content_hash
  - Still exactly one row (update in place, not a second insert)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_storage/test_upsert_from_upload.py::test_different_hash_updates_in_place
  status: active
  retired: null
- id: P5-DEPLOY-06
  behavior: A move/rename event updates the stored file's location, carrying its search-index
    entries along
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/event with type=moved, an old_path and a new_path
  fixtures: []
  steps:
  - Upload a file so a documents row (and its search entries) exist at the old path.
  - POST /api/event with type=moved, old_path, new_path, using a valid secret key.
  - 'Read back: `sqlite3 <db> "SELECT vault_path FROM documents WHERE vault_path =
    <new_path>"` and confirm the old path is gone.'
  expected:
  - The response is HTTP 200
  - The documents row now reads at new_path; no row remains at old_path
  - The search-index entries (keyword + meaning) for that note moved to new_path too
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestEventMoved::test_event_moved_updates_vault_path
  status: active
  retired: null
- id: P5-DEPLOY-07
  behavior: A delete event removes the stored file completely, including its search-index
    entries, within one transaction
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/event with type=deleted and a path
  fixtures: []
  steps:
  - Upload a file so its row and search entries exist.
  - POST /api/event with type=deleted and that path, using a valid secret key.
  - 'Confirm removal across all three tables: `sqlite3 <db> "SELECT COUNT(*) FROM
    documents WHERE vault_path = ?"` (and the two search tables).'
  expected:
  - The response is HTTP 200
  - No row remains for that path in documents, the keyword index, or the meaning index
  - Removal is hard delete (no soft-delete flag left behind)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestEventDeleted::test_event_deleted_removes_row
  status: active
  retired: null
- id: P5-DEPLOY-08
  behavior: An event naming a path that was never captured replies not_found instead
    of erroring
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/event (move or delete) for a path with no stored record
  fixtures: []
  steps:
  - With a fresh DB (or a path known to be absent), POST /api/event delete (or move)
    for that path using a valid secret key.
  expected:
  - The response is HTTP 200 with a body indicating not_found (e.g. {"status":"not_found"})
  - It is NOT treated as an error (no 4xx/5xx) — the file may simply not have been
    captured yet
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestEventNotFound::test_event_deleted_unknown_path_returns_not_found
  status: active
  retired: null
- id: P5-DEPLOY-09
  behavior: A request to a sync endpoint with a wrong or missing secret key is rejected
    as unauthorized
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: POST /api/upload or /api/event with a missing or incorrect Authorization
    bearer key
  fixtures: []
  steps:
  - POST to /api/upload with no Authorization header.
  - POST to /api/upload with an incorrect bearer key.
  expected:
  - Both requests return HTTP 401 (unauthorized)
  - No documents row is created or changed by the rejected requests
  - /health remains reachable without any key (the gate covers /api/* only)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_api.py::TestUnauthorized::test_upload_missing_key_returns_401
  status: active
  retired: null
- id: P5-DEPLOY-10
  behavior: First-ever start with no backup creates a fresh database with the correct,
    fully-migrated schema
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: container start with backup turned off (or no backup present in object
    storage)
  fixtures: []
  steps:
  - Start the container with no prior backup available.
  - 'Inspect the created database: `sqlite3 <db> "SELECT version FROM schema_version"`
    and `sqlite3 <db> "SELECT name FROM sqlite_master WHERE type=''table''"`.'
  expected:
  - A new database file is created at the dedicated data path
  - The schema is fully migrated (schema_version at the latest, all tables present
    including documents with full_body and the knowledge_entries table)
  - The app starts and serves requests against the fresh DB
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_cloud_entry.py::test_first_call_creates_db
  status: active
  retired: null
- id: P5-DEPLOY-11
  behavior: After a container restart with cloud backup configured, the database is
    restored from object storage rather than starting empty
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: stop and restart the container with backup settings present and a prior
    backup in object storage
  fixtures: []
  steps:
  - Start the container with backup configured; upload one or more files so rows exist
    and a backup streams up.
  - Stop the container; start it again with the same backup settings.
  - 'Confirm the rows survived: `sqlite3 <db> "SELECT COUNT(*) FROM documents"`'
  expected:
  - On the second start the database is restored from object storage before the app
    serves traffic
  - The previously-uploaded documents are present (no data loss across restart, modulo
    the accepted ~1 second crash window)
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: active
  retired: null
- id: P5-DEPLOY-12
  behavior: The existing stdio entry point for the knowledge-assistant interface still
    works unchanged for local desktop use
  tier: full
  phase: 5
  origin: design
  granularity: outcome
  trigger: python -m mcp_server.server (stdio transport, no container)
  fixtures: []
  steps:
  - Run the original stdio MCP entry point locally.
  - Send a tools/list request over stdio (as a desktop client would).
  expected:
  - The stdio server starts and responds with the registered tool list, exactly as
    before this slice
  - Adding the HTTP/cloud entry point did not break or alter the stdio path
  spec_ref: docs/1_design/P5_slice2_deployment_foundation.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: tests/test_mcp_server/test_cloud_entry.py::test_server_main_unchanged_after_cloud_entry_import
  status: active
  retired: null
- id: P7-CAP-10
  behavior: A binary/visual upload (raw bytes, no extracted text) stores the raw bytes
    in object storage and saves a document row that references the blob, BEFORE any
    AI runs — the file is safe the moment it is stored
  tier: full
  phase: 7
  origin: design
  granularity: outcome
  trigger: POST /api/upload (multipart, raw file bytes + mime + raw-byte fingerprint + size, no extracted_text)
  fixtures: []
  steps:
  - Upload one image file as raw bytes with a content fingerprint and no extracted text.
  - Inspect the object store and the document row for that path.
  expected:
  - The raw bytes are present in object storage under a content-addressed key (the
    object name is derived from the raw-byte fingerprint)
  - The document row holds a blob reference (object key) and the file type (mime), not
    the bytes themselves
  - The store-blob step completes before the vision model is invoked (store-first)
  - On a re-upload of identical bytes the blob is NOT stored again and the vision
    model is NOT invoked (front-loaded dedup over raw bytes)
  spec_ref: docs/1_design/phase7/phase7b_visual_capture.md §Success criteria
  human_reviewed: false
  last_tested: 2026-06-14
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_binary.py::test_describable_binary_upload_stores_blob_and_description, tests/test_pipelines/test_capture_binary.py::test_identical_reupload_skips_blob_and_ai
  status: active
  retired: null
- id: P7-CAP-11
  behavior: A describable binary (image or text-less PDF) gets one vision-model description
    written to both the summary and the full text fields, making it findable by search;
    on vision failure the blob is kept, the failure is audited, and success is still
    returned (store-anyway)
  tier: full
  phase: 7
  origin: design
  granularity: outcome
  trigger: POST /api/upload (multipart, describable image bytes)
  fixtures: []
  steps:
  - Upload one chart/image as raw bytes.
  - With the vision model healthy, confirm the description landed; then force a vision
    failure and confirm the blob and row survive.
  expected:
  - On success the document's summary and full-text fields hold the vision description
    and the title is descriptive (not the filename); the file is findable by keyword
    and meaning search
  - A success audit entry is written for the describe decision
  - On a forced vision failure the document row and the stored blob both survive, the
    description stays empty, a failure audit entry states why, and the endpoint still
    returns success
  spec_ref: docs/1_design/phase7/phase7b_visual_capture.md §Success criteria
  human_reviewed: false
  last_tested: 2026-06-14
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_binary.py::test_describable_binary_upload_stores_blob_and_description, tests/test_pipelines/test_capture_binary.py::test_vision_ai_failure_stores_blob_returns_success
  status: active
  retired: null
- id: P7-CAP-12
  behavior: A binary that is over the size cap, or of a type the vision model cannot
    read (zip, video, etc.), is stored but not described — the description stays empty
    and an audit entry records the reason (too-big / unsupported-type)
  tier: full
  phase: 7
  origin: design
  granularity: outcome
  trigger: POST /api/upload (multipart, oversized file OR unsupported binary type)
  fixtures: []
  steps:
  - Upload one file larger than the configured size cap, and separately one unsupported
    binary type (e.g. a zip).
  - Inspect the document row and the audit log.
  expected:
  - The blob is stored (the file is never lost) and the document row references it
  - The vision model is NOT invoked; the description stays empty (the same "needs-description"
    state as a vision failure — no new flag column)
  - An audit entry records the reason the description is missing (too-big or unsupported-type)
  - The endpoint returns success
  spec_ref: docs/1_design/phase7/phase7b_visual_capture.md §Success criteria
  human_reviewed: false
  last_tested: 2026-06-14
  last_result: passed
  pytest_ref: tests/test_pipelines/test_capture_binary.py::test_unsupported_type_stores_blob_no_description, tests/test_pipelines/test_capture_binary.py::test_over_size_cap_stores_blob_no_description
  status: active
  retired: null
- id: P7-CAP-13
  behavior: When a file delete is reported and its document row is removed, the stored
    blob is deleted only when no other document row still references it (delete-when-last-reference-gone);
    a blob shared by two rows survives the deletion of one
  tier: full
  phase: 7
  origin: design
  granularity: outcome
  trigger: POST /api/event (type=deleted) for a path whose row references a blob
  fixtures: []
  steps:
  - Capture two binary rows that point at the SAME content-addressed blob (identical
    bytes, two paths); delete one of them via the event endpoint.
  - Then delete the surviving row and re-check the object store.
  expected:
  - After deleting the first row, the blob is still present in object storage (a
    surviving row still references it)
  - After deleting the last referencing row, the blob is removed from object storage
  - Deleting a text row (no blob reference) removes only the document row and touches
    no object storage
  spec_ref: docs/1_design/phase7/phase7b_visual_capture.md §Success criteria
  human_reviewed: false
  last_tested: 2026-06-14
  last_result: passed
  pytest_ref: tests/test_mcp_server/test_api_blob_delete.py::TestLastRefDeleteRemovesBlob, tests/test_mcp_server/test_api_blob_delete.py::TestSharedRefDeleteKeepsBlob
  status: active
  retired: null
- id: P6-A2-01
  behavior: An unchanged file is recognised instantly from the local cache and is
    NOT re-extracted or re-uploaded — editing-then-saving a file with identical bytes,
    or restarting the daemon, skips work the cloud already has
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Save a watched file with no byte change, or restart the daemon over an
    already-synced vault
  fixtures: []
  steps:
  - Sync a vault, then touch/re-save a file so its bytes are unchanged, OR stop and
    restart the daemon.
  - Inspect the daemon logs and the cloud upload count for that file.
  expected:
  - The file's fingerprint in the local cache matches the file on disk, so the live
    path bails early (no extract, no upload) and the startup scan counts it as skipped
  - No new upload request is sent to the cloud for that file
  - The cloud document row for that file is untouched (same content_hash, same row)
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-02
  behavior: A fingerprint is written to the local cache ONLY after the cloud confirms
    receipt (cache-on-ack) — a failed or timed-out upload leaves the file uncached
    so the next reconcile re-detects and re-sends it
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Upload a changed file while the cloud returns an error/timeout, then let
    a reconcile run
  fixtures: []
  steps:
  - Force the cloud upload endpoint to fail (or time out) for one changed file; observe
    the cache.
  - Restore the cloud and trigger a reconcile (restart or periodic).
  expected:
  - After the failed upload the file has NO matching fingerprint in the local cache
    (the cache was not written on failure)
  - The next reconcile detects the file as changed/new and re-uploads it
  - After the successful upload the cache now holds the file's fingerprint
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-03
  behavior: A file dragged to a new folder is reconstructed as a single move (not a
    delete-then-recapture) by matching its content fingerprint — the cloud keeps the
    file's summary and knowledge, only updating its location
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Move a synced file between folders while the daemon is running (watcher
    emits delete+create or a native move)
  fixtures: []
  steps:
  - With the daemon running, drag a synced file from one folder to another.
  - Inspect what the cloud received and the document row's path/summary.
  expected:
  - The cloud receives ONE moved event (old path -> new path), not a delete plus a
    fresh upload
  - The document row's vault_path is updated to the new location and its summary /
    search entries are preserved (no re-summarize)
  - The local cache now keys the fingerprint under the new location
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-04
  behavior: A genuinely deleted file is reported as a delete only after the move-correlation
    window passes with no matching re-creation — a real delete still reaches the cloud,
    just a few seconds later (bias toward catching moves)
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Delete a synced file (no re-creation anywhere) while the daemon is running
  fixtures: []
  steps:
  - With the daemon running, delete a synced file and do not recreate it.
  - Wait past the move-correlation window and inspect the cloud.
  expected:
  - During the correlation window the delete is buffered (no delete event sent yet)
  - After the window passes with no matching create, exactly one deleted event is sent
    and the cloud document row is removed
  - The local cache entry for that file is removed
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-05
  behavior: A damaged or unreadable local cache is discarded whole and rebuilt from
    cloud truth — never partially trusted — so a corrupt notebook degrades to a full
    reconcile and loses no data
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Start the daemon with a cache file that is missing, garbled, or truncated
  fixtures: []
  steps:
  - Corrupt or truncate the cache file (or delete it), then start the daemon.
  - Inspect the startup behaviour and the resulting cache.
  expected:
  - The daemon does not crash and does not partially read the damaged cache; it starts
    with an empty cache
  - A full reconcile against the cloud runs, and the cache is rebuilt to match cloud
    truth (plus any disk-only files uploaded)
  - No cloud document rows are deleted purely because the cache was missing
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-06
  behavior: A file missing during one sweep is NOT instantly scrubbed from the cloud
    — a sweep-driven delete is only reported after the file stays gone across more
    than one check (conservative deletes; a transient sync hiccup never destroys real
    work)
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: A periodic/startup sweep finds a cloud-known file absent on disk for one
    check, then present again (transient) vs absent again (confirmed)
  fixtures: []
  steps:
  - Make a cloud-known file briefly invisible for one sweep, then restore it before
    the next sweep.
  - Separately, remove a cloud-known file and let it stay gone across two sweeps.
  expected:
  - A file absent in a single sweep is NOT reported as deleted; its cloud row survives
  - A file confirmed absent across more than one sweep is reported deleted and its
    cloud row is removed
  - A transiently-missing file that reappears is never deleted from the cloud
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-07
  behavior: A cloud rollback (e.g. Litestream restores an older backup that lost recent
    writes) self-heals on the next reconcile — files present on disk but now missing
    from the cloud are re-uploaded, with no data loss
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: Cloud state regresses (a previously-synced path disappears from GET /api/state)
    while the file still exists on disk; a reconcile runs
  fixtures: []
  steps:
  - Simulate a cloud rollback so a synced path is no longer in the cloud manifest while
    its file remains on disk and in the local cache.
  - Run a reconcile (startup or periodic).
  expected:
  - The disk-present / cloud-absent file is treated as new-to-cloud and re-uploaded
    (cloud authority drives the heal)
  - After the reconcile the cloud again knows the file and the cache matches cloud truth
  - The file is never deleted from disk and its content is never lost
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-08
  behavior: While the daemon runs, a periodic reconcile re-runs on a timer (config-gated,
    generous default) and catches files stranded by dropped watcher events — without
    needing a restart
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: A file change is missed by the live watcher (dropped event); the periodic
    reconcile interval elapses
  fixtures: []
  steps:
  - Arrange for a change the watcher misses (or simulate a stranded file), with the
    daemon left running.
  - Wait for the periodic reconcile interval to elapse.
  expected:
  - A reconcile runs on the timer without a restart and uploads the stranded/changed
    file
  - The cloud document row now reflects the change
  - With the interval disabled in config, no periodic reconcile runs
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-A2-09
  behavior: The live watcher and a running reconcile never corrupt the shared cache
    or double-process a file — concurrent access to the cache is serialised, and a
    file handled by one path is not re-handled by the other in the same window
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: A periodic reconcile runs at the same time the live watcher fires events
    on overlapping paths
  fixtures: []
  steps:
  - Trigger live file events on the same paths while a reconcile is in progress.
  - Inspect the cache integrity and the cloud upload counts for the overlapping files.
  expected:
  - The cache file/database is never left half-written or internally inconsistent under
    concurrent access
  - An overlapping file is not uploaded twice for the same change (no duplicate work
    from watcher + reconcile racing)
  - The final cache state matches the final disk state for the overlapping paths
  spec_ref: docs/1_design/phase6/P6_slice_A2_cache_reconcile.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-01
  behavior: The daemon's cloud key lives in the operating system's encrypted secret
    store (Keychain on Mac, Credential Manager on Windows) instead of a hand-set
    environment variable, and the daemon picks it up at startup with no plaintext
    file holding the secret
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The setup wizard stores a key, then the daemon process starts
  fixtures: []
  steps:
  - Store a key via the secret-store wrapper, then start a fresh daemon process.
  - Inspect that the key is read back and that no plaintext config file contains it.
  expected:
  - A key written to the OS secret store is readable back in a fresh process
  - The daemon's existing config loader receives the key without edits to its loader
  - No plaintext file (YAML config or dotenv) contains the secret
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-02
  behavior: The first-run setup hard-blocks on a live, authenticated connection test
    to the cloud before saving — a wrong key or unreachable address fails loudly at
    setup with a clear, named reason instead of saving a broken config that silently
    syncs nothing
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: A user enters a key + cloud address in the wizard and tries to finish
  fixtures: []
  steps:
  - Enter a correct key + reachable address and attempt to finish setup.
  - Repeat with a wrong key, then with an unreachable address.
  expected:
  - A correct key + reachable address passes and setup completes
  - A wrong key blocks completion with an error naming authentication
  - An unreachable address blocks completion with an error naming reachability
  - The test calls an authenticated endpoint, not the open health check, so a bad
    key cannot pass
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-03
  behavior: The two per-operating-system behaviours (start-at-login registration and
    tray registration) sit behind one shared interface with exactly one Mac and one
    Windows implementation; the correct one is selected automatically, and registering
    at login makes the app relaunch after a logout/login cycle
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The app runs on macOS or on Windows and registers itself to start at login
  fixtures: []
  steps:
  - Launch the app on each OS and let it register start-at-login.
  - Log out and back in; observe whether the app relaunches.
  expected:
  - The correct per-OS adapter is selected automatically with no manual switch
  - After a logout/login cycle the app relaunches on its own
  - Start-at-login is registered once on first successful setup, not on every launch
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-04
  behavior: A status tray icon is the user's only window into a headless background
    app — it shows whether the daemon is running, syncing, or in error, and offers
    Quit and Open-setup actions, without growing into a dashboard
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The app is running and the daemon's state changes (idle, syncing, error)
  fixtures: []
  steps:
  - Run the app and observe the tray icon while the daemon is idle, syncing, and in
    an error state.
  - Use the tray's Quit and Open-setup menu items.
  expected:
  - The tray icon appears and reflects running / syncing / error states
  - Quit and Open-setup menu items are present and wired
  - The tray shows status only — it is not a knowledge/dashboard view
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-05
  behavior: A non-technical user can complete first-run setup in a simple window with
    no terminal — choosing the notes folder, accepting or editing a pre-filled cloud
    address, and pasting the key; a failed connection test keeps the window open with
    a clear error and never writes settings
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: A user opens the app for the first time on a machine with no saved settings
  fixtures: []
  steps:
  - Open the app on an unconfigured machine and complete the wizard with valid input.
  - Repeat with a wrong key and observe the window and whether settings are written.
  expected:
  - Setup completes with no terminal use
  - The cloud-address field shows the baked default but accepts an edit
  - On success the config file exists in the shape the loader reads and the key is
    in the OS secret store
  - On a failed test the window stays open with a clear error and writes nothing
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-06
  behavior: One supervisor entry point decides setup-versus-run on launch, runs the
    existing sync engine on a background thread, shows the tray, and shuts down cleanly
    on Quit — a fresh machine shows the wizard then runs, an already-set-up machine
    runs with no window
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The app launches on a fresh machine and, separately, on an already-set-up
    machine
  fixtures: []
  steps:
  - Launch on an unconfigured machine, then on a configured one.
  - Choose Quit and confirm the engine stops and the process exits.
  expected:
  - A fresh machine shows the wizard then enters run mode
  - An already-set-up machine starts running with no window
  - The sync engine runs through the existing async run path (no new bare event loop)
  - Quit stops the engine cleanly and the process exits
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-07
  behavior: One generic installable file per operating system serves every user —
    the same binary supports different users with separate vaults and separate cloud
    runtimes, because vault path, key, and (editable) endpoint are all per-machine
    config, not baked per user
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The same downloaded installer is set up by two different users on two
    machines
  fixtures: []
  steps:
  - Install the same build on two machines and configure each with a different vault,
    key, and endpoint.
  - Confirm each runs independently against its own cloud runtime.
  expected:
  - Only the binary file is shared; vault path, key, and endpoint are per-machine
  - Two users with separate vaults and runtimes both run from the identical build
  - The baked endpoint is a pre-filled default, overridable per machine
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-08
  behavior: The packaged app is self-contained — it installs and runs on a clean Mac
    or Windows machine with no Python install, extracts PDF/DOCX/XLSX text inside the
    frozen bundle, loads the secret-store backend when frozen, and bundles no embedding
    model (those stay cloud-side, keeping the download small)
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The packaged installer is run on a clean machine with no developer tools
  fixtures: []
  steps:
  - Install from the DMG on a clean Mac and from the installer on a clean Windows box.
  - Drop PDF/DOCX/XLSX files and confirm text extraction and secret-store access work.
  expected:
  - The app installs and runs with no separate Python install
  - The frozen app extracts PDF/DOCX/XLSX (no raw-bytes fallback for these)
  - The secret-store backend loads in the frozen app
  - No embedding model ships in the bundle (confirmed — daemon imports no embedding
    library)
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-09
  behavior: Slice B adds zero sync or cache logic — the supervisor only starts the
    existing daemon and never reaches into its watch/extract/upload/reconcile/cache
    path, preserving the cache-advisory / cloud-authority contract
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The Slice B packaging/supervisor code is reviewed against the daemon's
    sync path
  fixtures: []
  steps:
  - Review the Slice B code for any watch, extract, upload, reconcile, or cache logic.
  expected:
  - The supervisor starts the daemon but contains no sync or cache logic of its own
  - The daemon's existing sync/cache behaviour is unchanged by Slice B
  - The cache-advisory / cloud-authority contract is not touched
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
- id: P6-SLICEB-10
  behavior: Uninstall leaves no trace — a dedicated cleanup step removes the key from
    the OS secret store, the config file, and the start-at-login registration; running
    it twice is safe
  tier: full
  phase: 6
  origin: design
  granularity: outcome
  trigger: The user uninstalls the app on a set-up machine
  fixtures: []
  steps:
  - Run the uninstall cleanup on a fully set-up machine.
  - Run it a second time and confirm no error.
  expected:
  - No key remains in the OS secret store after uninstall
  - No config file and no start-at-login registration remain
  - Running the cleanup twice is safe (idempotent) and reports what it removed
  spec_ref: docs/1_design/phase6/phase6_sliceB_installer.md §Success criteria
  human_reviewed: false
  last_tested: null
  last_result: null
  pytest_ref: null
  status: planned
  retired: null
